# Recon Phase Guide

Reconnaissance — discover hosts, domains, open ports and external-facing assets before engaging the target.

All commands below abstract their arguments through ``payload.json``. Set ``rhost``, ``lhost``, ``domain``, ``wordlist`` and credentials once; the framework substitutes them automatically. Never pass raw IP addresses or credentials as positional arguments.

## Commands

| Command | Description | Source |
|---------|-------------|--------|

| `do_alterx` | Executes the 'alterx' command for subdomain enumeration on the provided domain. If 'alterx' | `lazyown.py` |
| `do_apache_users` | Performs enumeration of users from a target system using `apache-users`. | `lazyown.py` |
| `do_binarycheck` | Performs various checks on a selected binary to gather information and protections. | `lazyown.py` |
| `do_cve` | Search for a CVE using the CIRCL API. | `lazyown.py` |
| `do_dig` | Executes the `dig` command to query DNS information. | `cli/commands/recon.py` |
| `do_dnschef` | Executes the DNSChef tool to monitor DNS queries and intercept responses. | `lazyown.py` |
| `do_dnsenum` | Performs DNS enumeration using `dnsenum` to identify subdomains for a given domain. | `cli/commands/recon.py` |
| `do_dnsmap` | Performs DNS enumeration using `dnsmap` to discover subdomains for a specified domain. | `cli/commands/recon.py` |
| `do_dnstool_py` | Executes the dnstool.py tool to modify Active Directory-integrated DNS records. | `lazyown.py` |
| `do_finalrecon` | Runs the `finalrecon` tool to perform a web server vulnerability scan against the specified target host. | `cli/commands/recon.py` |
| `do_getcap` | Retrieve and display file capabilities on the system. | `lazyown.py` |
| `do_gospider` | Try gospider for web spidering. | `lazyown.py` |
| `do_graudit` | Executes the graudit command to perform a static code analysis with the specified options. | `lazyown.py` |
| `do_httprobe` | Executes the httprobe tool to probe domains for working HTTP and HTTPS servers. | `lazyown.py` |
| `do_ipinfo` | Retrieves detailed information about an IP address using the ARIN API. | `lazyown.py` |
| `do_launchpad` | Searches for packages on Launchpad based on the provided search term and extracts codenames from the results. The distribution is extracted from the search ter… | `lazyown.py` |
| `do_metabigor` | Executes Metabigor commands for OSINT and scanning tasks with guided input or predefined arguments. | `lazyown.py` |
| `do_openssl_sclient` | Uses `openssl s_client` to connect to a specified host and port, allowing for testing and debugging of SSL/TLS connections. | `cli/commands/recon.py` |
| `do_ping` | Perform a ping to check host availability and infer the operating system based on TTL values. | `lazyown.py` |
| `do_ports` | Lists all open TCP and UDP ports on the local system. | `lazyown.py` |
| `do_proxy` | Runs a small proxy server to modify HTTP requests on the fly. | `lazyown.py` |
| `do_recon` | Performs reconnaissance on a specified domain using crt.sh (the target must be visible on internet), pup, httprobe, and EyeWitness. | `lazyown.py` |
| `do_serveralive2` | Command serveralive2: Uses Impacket to connect to a remote MSRPC interface and retrieves the server bindings. | `lazyown.py` |
| `do_sherlock` | Executes the Sherlock tool to find usernames across social networks. | `lazyown.py` |
| `do_sslscan` | Run an SSL scan on the specified remote host. | `lazyown.py` |
| `do_surface` | Render the network surface graph in the terminal. | `lazyown.py` |
| `do_tcpdump_capture` | Starts packet capture using `tcpdump` on the specified interface. | `lazyown.py` |
| `do_tcpdump_icmp` | Starts `tcpdump` to capture ICMP traffic on the specified interface. | `lazyown.py` |
| `do_trace` | Traces the DNS information for a given domain using the FreeDNS service. (using freedns IP Not your IP) | `lazyown.py` |
| `do_trufflehog` | Executes trufflehog to search for secrets in a given Git repository URL. | `lazyown.py` |
| `do_tshark_analyze` | Analyzes a packet capture file using `tshark` based on the provided remote host IP. | `lazyown.py` |
| `do_waybackmachine` | Fetch URLs from the Wayback Machine for a given website. | `lazyown.py` |
| `do_whatweb` | Performs a web technology fingerprinting scan using `whatweb`. | `cli/commands/recon.py` |
| `do_windapsearchscrapeusers` | Extracts usernames from a JSON output generated by go-windapsearch and appends them | `lazyown.py` |

## Next Phase

After completing the Recon phase, proceed to:
**Enumeration** (`docs/enum.md`)

---
*Generated from ``cli/command_index.json``. Keep this guide in sync by running ``python3 scripts/build_command_index.py`` after adding new commands.*

