"""Repository-side maintenance scripts.

Modules in this package are entry points for the developer workflow (index
generation, lint helpers, release prep). They are not imported by the
runtime: ``lazyown.py`` and ``lazyc2.py`` never depend on anything here.
"""
