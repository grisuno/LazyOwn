# Copy to .env and edit. .env is gitignored.

# Flask session secret. If unset, falls back to a hardcoded value concatenated
# with a per-startup UUID4. Set this in production to a random 64+ char string.
# Generate one with: python3 -c "import secrets; print(secrets.token_urlsafe(64))"
LAZYOWN_SECRET_KEY=
