name: generate_linux_raw_shellcode
description: >
  Genera shellcode en formato raw para una reverse shell TCP en Linux.
  No crea archivos, simplemente construye el shellcode en formato hexadecimal escapado listo para usar.
author: "LazyOwn RedTeam"
version: "1.0"
enabled: true
tags:
  - shellcode
  - reverse_shell
  - linux
  - raw
params:
  - name: lhost
    type: string
    required: true
    description: Dirección IP (LHOST) para el shellcode. Ejemplo 192.168.1.10
  - name: lport
    type: integer
    required: true
    description: Puerto (LPORT) para el shellcode. Ejemplo 4444
permissions: []
requires_root: false
dependencies: []
outputs: []
